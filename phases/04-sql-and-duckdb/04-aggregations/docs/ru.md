# Агрегации и уровни детализации

> GROUP BY меняет grain: это преобразование модели данных, а не только сокращение строк.

**Тип:** Case
**Треки:** Core
**Пререквизиты:** 04/03
**Время:** ~90 минут
**Результат:** объявляет grain результата GROUP BY и подтверждает агрегаты контролем.

## Цели обучения

- Определять grain результата по группирующим колонкам.
- Различать `COUNT(*)` и `COUNT(expression)`.
- Использовать `FILTER`, `WHERE` и `HAVING` на правильном уровне.
- Сверять агрегаты с ручным расчетом и аддитивными инвариантами.

## Проблема

Нужно посчитать оплаченную выручку по валютам. Итог `5005` выглядит убедительно, но без
объявленного grain непонятно, допустимы ли четыре строки результата, почему число заказов
не равно числу известных сумм и не потерялись ли строки с `NULL`.

## Концепция

`GROUP BY currency` создает одну группу на каждую валюту. Все выражения в `SELECT` должны
быть либо ключами группы, либо агрегатами. Grain результата: одна строка на `currency`.

Контрольные инварианты:

```text
sum(order_rows по группам) = число строк источника
sum(paid_revenue по валютам) = ручная сумма оплаченных заказов
currency уникальна в результате
```

## Соберите это

Пройдите оплаченные строки вручную и накопите словарь:

```text
RUB -> 1200 + 1500 = 2700
USD -> 75 + 60 + 45 = 180
EUR -> 25 + 900 + 700 = 1625
KZT -> 500
```

Итого `5005`. Эта таблица является независимым oracle для SQL.

```bash
uv run --locked python code/main.py
```

## Используйте это

```sql
SELECT
    currency,
    count(*) AS order_rows,
    count(amount) AS known_amount_rows,
    sum(amount) FILTER (WHERE status = 'paid') AS paid_revenue,
    count(*) FILTER (WHERE status = 'paid') AS paid_orders
FROM orders
GROUP BY currency;
```

`WHERE status = 'paid'` удалил бы прочие статусы для всех агрегатов. `FILTER` ограничивает
только выбранный агрегат и позволяет одновременно видеть полный размер группы.

## Сломайте это

1. Добавьте `status` в `SELECT`, но не в `GROUP BY`.
2. Замените `COUNT(*)` на `COUNT(amount)` и назовите колонку `orders`.
3. Сложите суммы разных валют без конвертации.
4. Примените фильтр агрегата в `WHERE` вместо `HAVING`.

Первые две ошибки либо остановят запрос, либо изменят смысл метрики; третья может дать
синтаксически корректное, но бессмысленное число.

## Проверьте это

Tiny-набор подтверждает четыре валюты, 12 исходных строк, 10 известных сумм и общую
оплаченную выручку `5005`.

```bash
uv run --locked python -m unittest discover -s tests
```

## Поставьте результат

```bash
uv run --locked python outputs/aggregate_model.py \
  --orders ../data/tiny/orders.csv
```

CLI поставляет агрегированную модель и checks, пригодные для regression test.

## Упражнения

1. Добавьте grain `currency, status`.
2. Посчитайте долю оплаченных заказов с явным знаменателем.
3. Добавьте `HAVING` для валют минимум с двумя заказами и проверьте новый grain.

## Ключевые термины

| Термин | Распространенное заблуждение | Точное значение |
|---|---|---|
| Group | Просто одинаковые значения | Множество строк с одной комбинацией ключей |
| Aggregate | Любая функция | Функция, сворачивающая строки группы |
| HAVING | Синоним WHERE | Фильтр уже построенных групп |
| FILTER | Фильтр всего запроса | Условие конкретного агрегата |
| Additivity | Возможность сложить любые числа | Допустимость суммирования по выбранным измерениям |

## Дополнительное чтение

- [DuckDB: GROUP BY](https://duckdb.org/docs/current/sql/query_syntax/groupby) — разберите стандартный синтаксис и `GROUP BY ALL`.
- [DuckDB: Aggregate Functions](https://duckdb.org/docs/current/sql/functions/aggregates) — изучите NULL-семантику `sum`, `avg`, `count` и порядок.
- [DuckDB: FILTER](https://duckdb.org/docs/current/sql/query_syntax/filter) — сравните условные агрегаты с `CASE`.
- [DuckDB: HAVING](https://duckdb.org/docs/current/sql/query_syntax/having) — прочитайте, когда условие относится к группе, а не исходной строке.
