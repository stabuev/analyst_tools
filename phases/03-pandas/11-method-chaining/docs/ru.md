# Method chaining и читаемые пайплайны

> Pipeline надёжен не потому, что помещается в одну цепочку. Он надёжен, когда порядок
> стадий виден, каждая стадия сохраняет свой контракт, а ошибка останавливает расчёт там,
> где впервые возникла.

**Тип:** Build
**Треки:** Core
**Пререквизиты:** 03/10
**Время:** ~75 минут
**Результат:** собирает уже проверенные преобразования в чистый pandas pipeline через
именованные стадии, `assign` и `pipe`, размещает checkpoints на границах контрактов и
сохраняет промежуточный результат, когда он нужен для диагностики.

## Цели обучения

После урока вы сможете:

- объяснить pipeline как порядок преобразований и проверок, а не как длинное выражение;
- описать контракт стадии: что она ожидает, что меняет и что обязана сохранить;
- сначала скомпоновать стадии обычными переменными, а затем перенести тот же порядок в
  `DataFrame.pipe`;
- использовать `assign` для связанных производных столбцов внутри одной стадии;
- строить pass-through checkpoint, который проверяет таблицу и возвращает её дальше без
  изменения;
- выбрать между продолжением цепочки и именованным промежуточным результатом;
- сохранить строгие правила dtype, пропусков, времени и категорий при композиции;
- проверить порядок отказов, отсутствие мутации и инварианты промежуточных таблиц.

## Где мы находимся в маршруте

В предыдущих уроках каждая операция рассматривалась отдельно:

```text
03/01  DataFrame, индекс, grain и бизнес-ключ
03/02  dtype-контракт: missing не равен неразбираемому значению
03/03  выбор строк и nullable-маски
03/04  производные столбцы и сохранение grain
03/05  агрегация и смена grain
03/06  merge, cardinality и unmatched-ключи
03/07  concat партий, provenance и порядок
03/08  wide/long reshape
03/09  UTC, explicit offset и локальный календарь
03/10  Unicode-нормализация и стабильные словари категорий
```

Теперь новых правил предметной области почти нет. Новая задача — соединить уже понятные
функции так, чтобы:

1. порядок действий был однозначным;
2. результат одной стадии удовлетворял входному контракту следующей;
3. нарушение не превращалось в правдоподобное число;
4. промежуточное состояние можно было проверить;
5. исходная таблица не менялась скрыто.

Следующий урок `03/12` соберёт из нескольких таблиц итоговую витрину и передаст её вместе
с машинным манифестом. Поэтому здесь мы работаем только с композицией функций в памяти:
чтение файлов, CLI и экспорт ещё не являются частью результата.

## Проблема

В `orders.csv` один и тот же подтверждённый статус записан тремя способами:

| `order_id` | сырое `status` | `amount` |
|---|---|---:|
| `O1001` | `paid` | 1200.00 |
| `O1003` | ` paid ` | 5500.00 |
| `O1007` | `PAID` | 75.50 |

Если сначала вычислить признак оплаты, а нормализовать строки позже, код выглядит
разумно:

```python
wrong = orders.assign(
    is_paid=orders["status"].eq("paid"),
    paid_amount=orders["amount"].where(
        orders["status"].eq("paid"),
        0,
    ),
)
```

Но `O1003` и `O1007` станут неоплаченными. Ошибка не вызовет исключение: сумма выручки
просто окажется правдоподобно заниженной.

Обратный порядок решает только часть проблемы:

```text
нормализовать status
-> проверить стабильный словарь
-> строго разобрать amount
-> только затем вычислить paid_amount
```

Если между этими шагами нет явных границ, внутри длинной цепочки легко потерять ещё три
контракта:

- `amount="oops"` после безусловного `errors="coerce"` станет пропуском;
- timestamp без offset после нестрогого разбора может быть принят за UTC;
- новый статус `completed` после сравнения с `paid` станет обычным `False`.

Во всех трёх случаях pipeline завершится и отдаст число. Поэтому нам нужна не просто
цепочка методов, а **проверяемая последовательность стадий**.

## Концепция

### Pipeline — это порядок стадий

В этом уроке pipeline — обычная последовательность функций:

```text
входная таблица
-> проверка raw-контракта
-> нормализация и типизация
-> checkpoint нормализованной таблицы
-> перевод времени
-> расчёт производных полей
-> checkpoint результата
-> воспроизводимая сортировка
```

Каждая стрелка передаёт таблицу следующей стадии. Запись через `pipe` лишь делает эту
передачу компактной. Она не исправляет неверный порядок и не добавляет проверок сама.

### Стадия отвечает за один уровень решения

Стадия — именованная функция `DataFrame -> DataFrame` с понятной ответственностью.

| Стадия | Что ожидает | Что меняет | Что сохраняет |
|---|---|---|---|
| `check_raw_orders` | обязательные столбцы и один заказ на строку | ничего | все строки, столбцы и индекс |
| `normalize_order_fields` | сырые status, currency и amount | текст, dtype и словарь status | grain и индекс |
| `check_normalized_orders` | объявленные dtype и словарь | ничего | весь вход без изменений |
| `add_time_columns` | timestamp с явным offset | добавляет UTC и локальную дату | строки, grain и индекс |
| `add_paid_metrics` | проверенные status и amount | добавляет `is_paid`, `paid_amount` | строки, grain и индекс |
| `check_prepared_orders` | все производные столбцы | ничего | весь вход без изменений |

Название `normalize_everything` было бы слабым: из него невозможно понять, где искать
ошибку status, amount или времени. Но дробить каждое выражение на отдельную функцию тоже
не нужно. Граница полезна, если у шага есть собственный входной контракт, отдельный
failure mode или самостоятельная проверка.

### Что означает «чистая стадия» в этом курсе

Для pandas-пайплайна нам достаточно практического контракта:

- все входы передаются аргументами;
- функция не читает файл и не обращается к глобальному изменяемому состоянию;
- исходный `DataFrame` не мутирует;
- одинаковый вход и параметры дают одинаковый результат;
- нарушение контракта вызывает предметную ошибку;
- функция либо возвращает новый `DataFrame`, либо проверяет и возвращает тот же объект
  без изменения.

Это не обещание, что pandas никогда не использует внутренние копии или оптимизации. Это
наблюдаемое правило для вызывающего кода: после вызова источник остаётся прежним, а
результат можно тестировать отдельно.

### Checkpoint — это проходная проверка

Checkpoint не обязан сохранять CSV. В нашем pipeline это функция вида:

```python
def check_something(frame: pd.DataFrame) -> pd.DataFrame:
    if contract_is_broken(frame):
        raise PipelineContractError("check_something: ...")
    return frame
```

Если контракт выполнен, функция возвращает таблицу дальше. Если нет — pipeline
останавливается на ближайшей осмысленной границе.

Хорошее имя ошибки содержит стадию:

```text
normalize_order_fields: unknown status values: ['completed']
```

По нему видно не только что сломалось, но и какая часть данных уже была обработана.

### `assign`, `pipe` и промежуточная переменная решают разные задачи

| Инструмент | Когда использовать | Когда не использовать |
|---|---|---|
| `assign` | добавить несколько связанных столбцов на том же grain | спрятать нормализацию, join и проверку в одном callback |
| `pipe` | передать таблицу именованной предметной функции | превратить каждую простую операцию в отдельную функцию |
| обычный метод | короткая очевидная операция вроде финальной сортировки | скрыть доменное правило в длинном выражении |
| переменная | результат нужно посмотреть, сравнить, закешировать или передать коллеге | единственная цель — искусственно избежать любой цепочки |

Читаемость не измеряется минимальным числом строк. Если нормализованную таблицу нужно
диагностировать, хорошее решение — дать ей имя.

### Порядок стадий является частью контракта

Две функции могут быть корректны по отдельности, но давать неверный процесс в обратном
порядке:

```text
normalize status -> validate vocabulary -> calculate paid metrics  # корректно
calculate paid metrics -> normalize status                         # неверно
```

Поэтому тесты pipeline проверяют не только функции, но и ошибки, которые должны быть
пойманы **до** необратимого расчёта.

## Соберите это

Сначала соберём процесс обычными присваиваниями. Так промежуточные состояния остаются
видимыми, а механизм композиции не скрывается за `pipe`.

### Шаг 1. Загрузите учебную таблицу

Из корня репозитория:

```python
from pathlib import Path

import pandas as pd

DATA = Path("phases/03-pandas/data/tiny/orders.csv")
orders = pd.read_csv(DATA)
```

До преобразований сформулируйте grain:

```text
одна строка -> один заказ
кандидатный бизнес-ключ -> order_id
строк ожидается после сохраняющих стадий -> столько же, сколько во входе
```

### Шаг 2. Зафиксируйте карточки стадий

Не начинайте с синтаксиса цепочки. Для каждой стадии ответьте на четыре вопроса:

```text
1. Какие столбцы и dtype она ожидает?
2. Что именно она добавляет или изменяет?
3. Должны ли сохраниться строки, grain и индекс?
4. Какая ошибка обязана остановить процесс здесь?
```

Для `normalize_order_fields` карточка выглядит так:

```text
вход:  raw status, currency, amount
выход: categorical status, string currency, Float64 amount
сохраняет: число строк, order_id, индекс
останавливает: missing/unknown status, неверный код currency,
              непустой неразбираемый amount
```

Эта запись важнее `.pipe(...)`: она определяет смысл стадии независимо от формы вызова.

### Шаг 3. Соберите последовательность обычными переменными

Загрузите функции из своего рабочего файла. Ниже важен интерфейс функций, а не имя
модуля:

```python
from order_pipeline import (
    add_paid_metrics,
    add_time_columns,
    check_normalized_orders,
    check_prepared_orders,
    check_raw_orders,
    normalize_order_fields,
)
```

Готовый reference-артефакт запускается через `code/main.py`; там загрузка файла из папки
урока уже настроена.

Теперь соедините стадии без method chaining:

```python
source = orders.copy(deep=True)
source_index = source.index.copy()
source_rows = len(source)

raw_checked = check_raw_orders(source)
normalized = normalize_order_fields(raw_checked)
normalized_checked = check_normalized_orders(
    normalized,
    expected_index=source_index,
)
with_time = add_time_columns(
    normalized_checked,
    timezone="Europe/Moscow",
)
with_metrics = add_paid_metrics(with_time)
prepared = check_prepared_orders(
    with_metrics,
    expected_index=source_index,
    expected_rows=source_rows,
)
result = (
    prepared
    .sort_values("order_id", kind="stable")
    .reset_index(drop=True)
)
```

Это уже pipeline. Method chaining пока не используется, но:

- порядок стадий явный;
- результат каждой стадии можно открыть;
- каждая функция получает результат предыдущей;
- checkpoints находятся рядом с контрактами;
- финальная сортировка отделена от сохраняющих стадий.

### Шаг 4. Посмотрите на границу, а не только на финал

Например, после `normalize_order_fields` проверьте:

```python
assert normalized.index.equals(source_index)
assert len(normalized) == source_rows
assert str(normalized["amount"].dtype) == "Float64"
assert normalized["status"].cat.categories.tolist() == [
    "paid",
    "refunded",
    "pending",
    "cancelled",
]
```

Если эта таблица нужна для диагностики, переменная `normalized` полезна и в рабочем коде.
Не нужно удалять её только ради визуально непрерывной цепочки.

### Шаг 5. Предскажите отказ до запуска

Для каждого входа назовите стадию, которая должна остановить процесс:

| Изменение | Ожидаемая стадия отказа | Почему нельзя продолжать |
|---|---|---|
| повтор `order_id` | `check_raw_orders` | grain нарушен до расчётов |
| `amount="oops"` | `normalize_order_fields` | invalid нельзя превращать в missing |
| `status="completed"` | `normalize_order_fields` | unknown нельзя считать `False` |
| timestamp без offset | `add_time_columns` | неизвестно, где находится момент на UTC timeline |
| paid-заказ без amount | `check_prepared_orders` | выручка для оплаченного заказа неизвестна |

Такое предсказание превращает pipeline из набора вызовов в проверяемую модель процесса.

## Используйте это

Теперь заменим только способ передачи таблицы между теми же функциями.

### `pipe` передаёт текущий DataFrame первым аргументом

Reference-функция `prepare_orders` содержит:

```python
def prepare_orders(frame: pd.DataFrame, *, timezone: str) -> pd.DataFrame:
    source = frame.copy(deep=True)
    source_index = source.index.copy()
    source_rows = len(source)

    return (
        source.pipe(check_raw_orders)
        .pipe(normalize_order_fields)
        .pipe(
            check_normalized_orders,
            expected_index=source_index,
        )
        .pipe(add_time_columns, timezone=timezone)
        .pipe(add_paid_metrics)
        .pipe(
            check_prepared_orders,
            expected_index=source_index,
            expected_rows=source_rows,
        )
        .sort_values("order_id", kind="stable")
        .reset_index(drop=True)
    )
```

Вызов:

```python
frame.pipe(add_time_columns, timezone="Europe/Moscow")
```

эквивалентен:

```python
add_time_columns(frame, timezone="Europe/Moscow")
```

`pipe` не меняет поведение функции. Он делает направление потока данных видимым сверху
вниз и позволяет читать pipeline как список предметных стадий.

### `assign` остаётся внутри стадии

Временная стадия сначала строго разбирает timestamp, проверяет offset и только потом
добавляет связанные столбцы:

```python
return frame.assign(
    ordered_at_utc=parsed,
    local_order_date=local.dt.date,
)
```

`assign` возвращает новый `DataFrame`, поэтому вызывающий код не получает скрытую
мутацию. Выражения вычисляются по порядку объявления; более поздний callable может
использовать столбец, созданный выше в том же `assign`. Но длинную цепочку зависимых
вычислений лучше разделить, если отдельный результат имеет собственный контракт.

### Простые методы не обязаны становиться стадиями

Финальные:

```python
.sort_values("order_id", kind="stable")
.reset_index(drop=True)
```

остаются обычными методами. Их эффект короток и понятен: задать воспроизводимый порядок
и создать технический индекс результата. Функция `sort_and_reset_everything` не добавила
бы предметного смысла.

### Когда цепочку нужно разорвать

Сохраните промежуточный результат в переменную, если нужно:

- посмотреть распределение после нормализации;
- сравнить таблицу до и после merge;
- сохранить evidence для handoff;
- повторно использовать дорогой результат;
- передать таблицу в две разные ветки анализа;
- локализовать ошибку, которую одного сообщения недостаточно объяснить.

Например:

```python
normalized = (
    source.pipe(check_raw_orders)
    .pipe(normalize_order_fields)
    .pipe(check_normalized_orders, expected_index=source.index)
)

inspect_status_distribution(normalized)

result = (
    normalized.pipe(add_time_columns, timezone="Europe/Moscow")
    .pipe(add_paid_metrics)
)
```

Это не менее «правильный» pipeline. Переменная обозначает важную границу процесса.

### Почему в обязательном маршруте нет `query`

`DataFrame.query` может быть удобен для некоторых фильтров, но он не решает новую задачу
композиции. У него отдельный язык строковых выражений и собственные правила обращения к
внешним переменным. В `03/03` уже изучены обычные и nullable-маски, где индекс и политика
`NA` видны напрямую.

Поэтому в этом уроке фильтрация остаётся через знакомые маски, а `query` не является
скрытым prerequisite. При необходимости его можно изучить как дополнительный интерфейс,
не меняя модель pipeline.

## Сломайте это

### 1. Рассчитайте метрику до нормализации status

```python
broken = orders.assign(
    is_paid=lambda value: value["status"].eq("paid"),
)
```

Код не падает, но пропускает `" paid "` и `"PAID"`. Это опаснее явного исключения:
неверный ответ выглядит реалистично.

### 2. Превратите invalid amount в missing

```python
amount = pd.to_numeric(orders["amount"], errors="coerce")
```

`errors="coerce"` допустим как часть аудита, если после него сравниваются исходная
непустота и результат разбора. Без такой проверки `"oops"` становится неотличимым от
настоящего пропуска. Pipeline должен остановиться на `normalize_order_fields`.

### 3. Примите naive timestamp за UTC

```python
pd.to_datetime(
    pd.Series(["2026-02-01 10:00:00"]),
    utc=True,
)
```

Параметр `utc=True` не сообщает исходный часовой пояс. Строка без `Z` или `+03:00`
нарушает контракт `03/09` и должна быть отклонена во временной стадии.

### 4. Посчитайте unknown status как `False`

```python
pd.Series(["completed"], dtype="string").eq("paid")
```

Результат `False` технически верен для сравнения строк, но предметно ещё неизвестно,
что означает новый статус. Сначала нужно сверить стабильный словарь, затем считать
метрику.

### 5. Измените вход inplace

```python
def bad_stage(frame):
    frame["status"] = frame["status"].str.strip().str.lower()
    return frame
```

После вызова исходный notebook-объект уже изменён. Повторный запуск ячейки работает на
другом входе, а тесты зависят от порядка. Верхнеуровневая функция копирует источник, а
преобразующие стадии используют `assign`.

### 6. Незаметно переставьте строки внутри сохраняющей стадии

Функция может не удалить строки, но изменить порядок или метки индекса. Следующая
операция с внешней `Series` выровняет значения по меткам, а не по видимой позиции.
`check_normalized_orders` поэтому сверяет весь индекс, а не только `len(frame)`.

### 7. Спрячьте несколько уровней абстракции в одной функции

```text
clean_and_join_and_group_and_export(...)
```

Такую функцию трудно проверить на границах. Нормализация, смена grain, join и I/O имеют
разные контракты и failure modes; им нужны отдельные стадии.

## Проверьте это

Запустите behavioral tests урока:

```bash
uv run --locked python phases/03-pandas/11-method-chaining/tests/test_main.py
```

Проверки подтверждают наблюдаемое поведение:

- число заказов и уникальный `order_id` сохраняются;
- финальный порядок детерминирован;
- status получает стабильный категориальный словарь;
- amount, currency и `is_paid` имеют объявленные nullable dtype;
- offsets переводятся в UTC, затем в локальный календарный день;
- источник не мутирует;
- duplicate и blank key останавливаются на raw-checkpoint;
- invalid amount не превращается в missing;
- missing и unknown status не превращаются в неоплаченный заказ;
- naive и неразбираемый timestamp останавливаются во временной стадии;
- paid-заказ без amount не даёт молча заниженную сумму;
- checkpoint замечает перестановку промежуточных строк.

### Карта инвариантов

| Граница | Что должно быть истинно |
|---|---|
| после `check_raw_orders` | обязательные столбцы; `order_id` заполнен и уникален |
| после `normalize_order_fields` | status из стабильного словаря; currency — `string`; amount — `Float64` |
| после `check_normalized_orders` | индекс и порядок строк совпадают с источником |
| после `add_time_columns` | непустые timestamps имели offset и находятся на UTC timeline |
| после `add_paid_metrics` | метрика рассчитана только из проверенного status |
| после `check_prepared_orders` | paid сохраняет amount; non-paid даёт 0; paid без amount запрещён |
| после сортировки | порядок воспроизводим; технический индекс создан заново |

Важно: финальная проверка не заменяет промежуточные. Если invalid amount обнаружить лишь
после расчёта выручки, часть информации об источнике уже потеряна.

## Поставьте результат

Артефакт `outputs/order_pipeline.py` экспортирует:

- `prepare_orders` — верхнеуровневый pipeline;
- именованные преобразующие стадии;
- pass-through checkpoints;
- `PipelineContractError` с названием ближайшей стадии.

Запустите reference-пример:

```bash
uv run --locked python phases/03-pandas/11-method-chaining/code/main.py
```

Использование как функции:

```python
prepared_orders = prepare_orders(
    raw_orders,
    timezone="Europe/Moscow",
)
```

Функция не читает и не записывает файлы. Это сознательная граница: в `03/12` тот же тип
pipeline станет частью сборки и экспорта витрины, где I/O получит отдельный контракт.

## Упражнения

1. Добавьте в `orders` строку с `amount="unknown"`. До запуска предскажите стадию и текст
   ошибки, затем подтвердите, что источник не изменился.
2. Реализуйте стадию `add_amount_band`, которая через `assign` добавляет категории
   `small`, `medium`, `large`, но не меняет grain и индекс. Добавьте checkpoint стабильного
   словаря.
3. Напишите неправильную стадию, которая сортирует строки внутри нормализации. Докажите,
   что `check_normalized_orders` обнаруживает перестановку.
4. Разорвите reference-цепочку после нормализации, посчитайте распределение status и
   продолжите pipeline. Объясните, почему переменная здесь улучшает, а не ухудшает код.
5. Поменяйте местами нормализацию status и расчёт `is_paid`. Создайте тест, который ловит
   правдоподобно заниженную сумму на `" paid "` и `"PAID"`.
6. Перенесите одну чистую функцию из предыдущего урока в собственный pipeline. Запишите
   её входной, выходной и сохраняемый контракты до написания `.pipe(...)`.

## Что сознательно остаётся за границей урока

- `DataFrame.query` и `eval` как отдельный язык выражений;
- полный каталог возможностей `pipe`;
- декораторы, custom accessors и собственный fluent API;
- orchestration-системы, DAG scheduler, retries и распределённое выполнение;
- логирование, кэширование и materialization промежуточных таблиц;
- чтение файлов, CLI и экспорт — это `03/12`;
- производительность больших pipeline — отдельная фаза 12;
- data quality framework — систематически появится в фазе 07.

Сейчас важен минимальный переносимый принцип: сначала корректные локальные функции, затем
явный порядок, checkpoints и только после этого удобная форма записи.

## Ключевые термины

| Термин | Распространённое заблуждение | Точное значение |
|---|---|---|
| Pipeline | Одно длинное выражение | Упорядоченная последовательность преобразований и проверок |
| Stage | Любой вызов метода pandas | Именованный шаг с входным, выходным и сохраняемым контрактом |
| Method chain | Чем длиннее, тем профессиональнее | Последовательность вызовов, полезная только пока она читаема |
| Pure stage | Функция обязательно без единой копии | Стадия без скрытого состояния и наблюдаемой мутации входа |
| `pipe` | Автоматическая проверка pipeline | Передача текущего DataFrame именованной функции |
| `assign` | Любая очистка таблицы | Создание связанных столбцов с возвратом нового DataFrame |
| Checkpoint | Обязательно файл на диске | Pass-through функция, проверяющая инвариант между стадиями |
| Invariant | Утверждение только о финальном результате | Свойство, которое должно быть истинно на конкретной границе |
| Intermediate result | Признак плохого chaining | Именованное состояние для диагностики, ветвления или handoff |
| Failure localization | Полный traceback | Остановка на ближайшей предметной стадии с понятной причиной |

## Дополнительное чтение

- [pandas: Tablewise function application](https://pandas.pydata.org/docs/user_guide/basics.html#tablewise-function-application) — начните с подраздела **Tablewise function application**.
  Сравните обычную вложенную композицию `h(g(f(df)))` с последовательностью `pipe` и
  зафиксируйте главный контракт: каждая функция принимает и возвращает всю таблицу.
- [pandas: `DataFrame.pipe`](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.pipe.html) — разберите сигнатуру, `args`, `kwargs` и основной пример.
  Tuple-форму `(callable, data_keyword)` рассматривайте только как адаптер для чужой
  функции, у которой таблица не является первым аргументом, а не как обязательный стиль.
- [pandas: `DataFrame.assign`](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.assign.html) — изучите разделы **Parameters**, **Notes** и примеры нескольких новых столбцов.
  Обратите внимание: callables не должны менять вход, а более поздний столбец может
  использовать созданный ранее; сопоставьте это с границей отдельной стадии в уроке.
- [Яндекс Образование: модуль `pandas`](https://education.yandex.ru/handbook/python/article/modul-pandas) — найдите фрагмент о `assign()` после примера сортировки по новой колонке.
  Русскоязычный пример закрепляет, что `assign` возвращает новую таблицу; сравните
  короткий расчёт `total_score` с доменной функцией, которой уже нужен `pipe`.
- [Хабр: «Цепочка методов в pandas. Прокачиваем свой код»](https://habr.com/ru/articles/722170/) — прочитайте разделы о `pipe` и `assign`, затем критически разберите начало большого примера.
  Не копируйте длину цепочки и `query` как норму: отметьте места, где именованная стадия,
  checkpoint или промежуточная переменная сделали бы failure mode заметнее.
- [pandas: Copy-on-Write](https://pandas.pydata.org/docs/user_guide/copy_on_write.html) — прочитайте **Description**, **Chained Assignment** и **Patterns to avoid**.
  Материал объясняет поведение копий в pandas 3.0, но не отменяет контракт чистой стадии:
  отсутствие мутации входа всё равно нужно проверять наблюдаемым тестом.
- [pandas: `assert_frame_equal`](https://pandas.pydata.org/docs/reference/api/pandas.testing.assert_frame_equal.html) — разберите `check_dtype`, проверки индекса, категорий и пример несовпадающих dtype.
  Используйте страницу как продолжение раздела Verify It: равенство значений без проверки
  формы, меток и типов недостаточно для сохраняющей стадии.
- [Microsoft Learn: основные понятия функционального программирования](https://learn.microsoft.com/ru-ru/dotnet/fsharp/tutorials/functional-programming-concepts) — прочитайте только русскоязычные разделы **Чистые функции** и **Неизменность**.
  Синтаксис F# здесь вторичен: перенесите два свойства — одинаковый результат для одного
  входа и отсутствие побочных эффектов — на функцию `DataFrame -> DataFrame`.
- [Python: Functional Programming HOWTO](https://docs.python.org/3/howto/functional.html) — ограничьтесь введением и подразделами **Modularity**, **Ease of debugging and testing** и **Composability**.
  Они дают общую причину дробить процесс на небольшие функции; каталог итераторов и
  `itertools` для этого урока не нужен.
- [Tom Augspurger: Modern Pandas — Method Chaining](https://tomaugspurger.net/posts/method-chaining/) — прочитайте **Method Chaining**, `pipe` и особенно **Costs**.
  Это материал для углубления и исторического контекста: API-примеры сверяйте с текущей
  документацией, а тезис о трудной диагностике длинных цепочек — с правилом именовать
  важные промежуточные результаты.
