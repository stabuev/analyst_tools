{
  "version": "2.0.0",
  "query": {
    "bind_names": ["fetch_limit", "min_amount", "status"]
  },
  "source": {
    "tables": {
      "orders": {
        "required_columns": ["order_id", "user_id", "ordered_at", "amount", "status"],
        "primary_key": ["order_id"],
        "non_nullable": ["order_id", "user_id", "ordered_at", "amount", "status"]
      },
      "users": {
        "required_columns": ["user_id", "segment"],
        "primary_key": ["user_id"],
        "non_nullable": ["user_id", "segment"]
      }
    }
  },
  "result": {
    "columns": ["order_id", "user_id", "ordered_at", "amount", "status", "segment"],
    "grain": ["order_id"],
    "fields": {
      "order_id": {"type": "string", "nullable": false},
      "user_id": {"type": "string", "nullable": false},
      "ordered_at": {"type": "string", "nullable": false},
      "amount": {"type": "number", "nullable": false},
      "status": {"type": "string", "nullable": false},
      "segment": {"type": "string", "nullable": false}
    },
    "domains": {
      "status": ["paid", "cancelled", "refunded"]
    },
    "relationship_fields": ["segment"],
    "allow_empty": true
  }
}
