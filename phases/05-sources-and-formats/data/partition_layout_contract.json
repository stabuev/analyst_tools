{
  "version": "1.0.0",
  "source": {
    "columns": [
      {"name": "order_id", "type": "string", "nullable": false},
      {"name": "user_id", "type": "string", "nullable": false},
      {"name": "ordered_at", "type": "timestamp[us, tz=UTC]", "nullable": false},
      {"name": "amount", "type": "decimal128(12, 2)", "nullable": false},
      {"name": "currency", "type": "string", "nullable": false},
      {"name": "comment", "type": "string", "nullable": true}
    ],
    "grain": ["order_id"],
    "row_count": 5,
    "null_counts": {
      "order_id": 0,
      "user_id": 0,
      "ordered_at": 0,
      "amount": 0,
      "currency": 0,
      "comment": 2
    }
  },
  "derived_columns": [
    {"name": "order_month", "source": "ordered_at", "transform": "utc_month"},
    {"name": "order_date", "source": "ordered_at", "transform": "utc_date"}
  ],
  "candidates": [
    {"name": "month", "partition_by": ["order_month"]},
    {"name": "month_currency", "partition_by": ["order_month", "currency"]},
    {"name": "day_currency", "partition_by": ["order_date", "currency"]},
    {"name": "order", "partition_by": ["order_id"]}
  ],
  "selected": "month_currency",
  "workload": [
    {"name": "monthly_orders", "filters": {"order_month": "2026-05"}},
    {"name": "currency_orders", "filters": {"currency": "EUR"}},
    {
      "name": "monthly_currency_orders",
      "filters": {"order_month": "2026-05", "currency": "EUR"}
    }
  ],
  "diagnostics": {
    "small_partition_rows": 2,
    "allow_null_partition_values": false
  }
}
