{
  "name": "parameterized-db-reader",
  "type": "cli",
  "path": "outputs/db_reader.py",
  "description": "Читает срез через caller-owned SQLAlchemy Engine и reviewed order_slice.sql, проверяет source schema, JOIN, completeness и grain и атомарно публикует только валидный snapshot.",
  "usage": "python outputs/db_reader.py --database ../data/tiny/analytics.sqlite --min-amount 100 --status paid --output delivery/orders_snapshot.json"
}
