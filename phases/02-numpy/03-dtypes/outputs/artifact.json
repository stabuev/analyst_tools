{
  "name": "numpy-dtype-audit",
  "type": "cli",
  "path": "outputs/dtype_audit.py",
  "description": "Проверяет dtype по observed и expected range, пропускам, допустимой ошибке и памяти планируемого массива.",
  "usage": "uv run --locked python outputs/dtype_audit.py --values '[0, 12, 200]' --target-dtype uint16 --expected-min 0 --expected-max 20000 --planned-shape '[500, 365, 24]'"
}
