# Dtype, память и диапазоны

> `dtype` является частью контракта данных: экономия памяти полезна только тогда, когда диапазон, пропуски и точность результата остаются допустимыми.

**Тип:** Build
**Треки:** Core
**Пререквизит:** `02-numpy/02-shape-and-axes`
**Время:** ~60 минут
**Результат:** выбирает dtype с учетом диапазона, памяти, пропусков и точности.

## Цели обучения

- вывести диапазон знакового и беззнакового целого типа;
- связать `itemsize`, `size` и `nbytes`;
- использовать `np.iinfo` и `np.finfo`;
- учитывать пропуски и потерю точности при выборе `dtype`;
- обнаруживать значения, не помещающиеся в запрошенный тип.

## Проблема

Массив из десяти миллионов идентификаторов типа `int64` занимает примерно 80 МБ.
Если все значения лежат между `0` и `255`, `uint8` потребует около 10 МБ.

Но механическое уменьшение типа опасно:

- значение `256` не помещается в `uint8`;
- отрицательное значение не помещается в беззнаковый тип;
- `NaN` нельзя представить обычным integer dtype;
- `float32` может быть недостаточно точным для финансового итога.

Нужен не самый маленький тип, а самый маленький тип, сохраняющий контракт задачи.

## Концепция

`dtype` описывает представление одного элемента. Для массива:

```text
nbytes = size * itemsize
```

```python
import numpy as np

values = np.zeros(1_000, dtype=np.uint8)

assert values.itemsize == 1
assert values.nbytes == 1_000
```

### Диапазон целого типа

Для беззнакового целого из `b` бит:

```text
minimum = 0
maximum = 2**b - 1
```

Для знакового целого в дополнительном коде:

```text
minimum = -(2**(b - 1))
maximum = 2**(b - 1) - 1
```

Поэтому:

| dtype | Минимум | Максимум | Байт |
|---|---:|---:|---:|
| `uint8` | 0 | 255 | 1 |
| `int8` | -128 | 127 | 1 |
| `uint16` | 0 | 65 535 | 2 |
| `int16` | -32 768 | 32 767 | 2 |

### Числа с плавающей точкой

`float32` и `float64` различаются не только памятью. У них разные:

- диапазоны конечных значений;
- расстояния между соседними представимыми числами;
- машинные эпсилоны;
- ошибки накопления.

`float32` занимает четыре байта, `float64` восемь, но решение нельзя принимать только по
памяти.

## Соберите это

### Ручной диапазон integer dtype

```python
def integer_bounds(bits, signed):
    if signed:
        return -(2 ** (bits - 1)), 2 ** (bits - 1) - 1
    return 0, 2**bits - 1
```

```python
assert integer_bounds(8, signed=True) == (-128, 127)
assert integer_bounds(8, signed=False) == (0, 255)
```

### Выбор минимального типа

Алгоритм должен учитывать минимум и максимум данных:

```python
def fits(minimum, maximum, lower, upper):
    return minimum >= lower and maximum <= upper
```

Для диапазона `0..255` подходит `uint8`. Для `-1..127` нужен `int8`, а для
`-129..128` уже `int16`.

Рекомендация по диапазону еще не является окончательным решением. Нужно отдельно
проверить:

- присутствуют ли пропуски;
- действительно ли значения целые;
- появятся ли более широкие значения после следующего преобразования;
- является ли выбранный тип контрактом внешней системы.

## Используйте это

NumPy предоставляет точные параметры типов:

```python
uint8 = np.iinfo(np.uint8)

assert uint8.min == 0
assert uint8.max == 255
assert uint8.bits == 8
```

Для floating dtype:

```python
float32 = np.finfo(np.float32)

print(float32.eps)
print(float32.smallest_normal)
print(float32.max)
```

Создайте массив с явным типом:

```python
statuses = np.array([0, 1, 1, 0], dtype=np.uint8)
```

Память легко сравнить:

```python
wide = np.zeros(1_000_000, dtype=np.int64)
narrow = np.zeros(1_000_000, dtype=np.uint8)

assert wide.nbytes == 8_000_000
assert narrow.nbytes == 1_000_000
```

### Пропуски

```python
measurements = np.array([12.5, np.nan, 18.0])
assert measurements.dtype == np.float64
```

`NaN` является floating-point значением. Если предметная модель требует nullable integer,
на следующей фазе pandas предложит отдельные nullable dtype. В обычном `ndarray` следует
явно выбрать стратегию: float с `NaN`, маска, sentinel или отдельный массив валидности.

### Аудитор

```bash
uv run --locked python phases/02-numpy/03-dtypes/outputs/dtype_audit.py \
  --values '[0, 12, 255]' \
  --dtype uint8
```

Отчёт содержит фактический `dtype`, диапазон, `itemsize`, `nbytes` и минимальный
целочисленный тип по наблюдаемым значениям.

```bash
uv run --locked python phases/02-numpy/03-dtypes/outputs/dtype_audit.py \
  --values '[12.5, null, 18.0]'
```

Здесь аудитор покажет один пропуск и не разрешит применить integer-рекомендацию.

## Сломайте это

### Выход за диапазон

```bash
uv run --locked python phases/02-numpy/03-dtypes/outputs/dtype_audit.py \
  --values '[0, 256]' \
  --dtype uint8
```

Современный NumPy отклоняет такое создание вместо молчаливого принятия неверного
значения. Но арифметика уже существующих integer-массивов всё равно требует отдельной
проверки на переполнение.

### Отрицательное значение в unsigned

```python
np.array([-1], dtype=np.uint8)
```

Отрицательное наблюдение нарушает контракт беззнакового типа, даже если большинство
значений положительные.

### Пропуск в integer dtype

```python
np.array([1, np.nan, 3], dtype=np.int64)
```

Преобразование невозможно: `NaN` не имеет целочисленного представления.

### Неявное понижение точности

```python
values = np.array([0.1, 0.2, 0.3], dtype=np.float32)
```

Такой тип может быть достаточен для признаков модели и недостаточен для итогового
финансового расчёта. Допуск должен следовать из задачи и проверяться тестом.

## Проверьте это

```bash
uv run --locked python -m unittest discover \
  -s phases/02-numpy/03-dtypes/tests \
  -v
```

Проверки покрывают ручные диапазоны, выбор минимального типа, память, пропуски,
нечисловые данные и выход за диапазон.

```bash
uv run --locked python phases/02-numpy/03-dtypes/code/main.py
```

## Поставьте результат

`outputs/dtype_audit.py` можно использовать перед загрузкой большого числового массива:

```bash
uv run --locked python phases/02-numpy/03-dtypes/outputs/dtype_audit.py \
  --values '[[0, 1, 1], [1, 0, 1]]' \
  --output /tmp/dtype-report.json
```

Рекомендация артефакта ограничена наблюдаемыми значениями. Перед изменением production
схемы добавьте бизнес-границы будущих данных и проверьте все последующие операции.

## Упражнения

1. Сравните `nbytes` одного миллиона элементов `int64`, `int32`, `int16` и `int8`.
2. Добавьте в аудитор аргументы `--expected-min` и `--expected-max`, чтобы рекомендация
   учитывала не только текущую выборку.
3. Добавьте проверку преобразования `float64 -> float32`, которая сообщает максимальную
   абсолютную ошибку round-trip.

## Ключевые термины

| Термин | Распространенное заблуждение | Точное значение |
|---|---|---|
| `dtype` | «Тип Python внутри каждой ячейки» | Фиксированное представление элементов ndarray |
| `itemsize` | «Число элементов» | Размер одного элемента в байтах |
| `nbytes` | «Полная память процесса» | Объём буфера элементов массива |
| Signed integer | «Любое целое» | Тип, часть диапазона которого отдана отрицательным значениям |
| Unsigned integer | «Более точный integer» | Неотрицательный целый тип с расширенным верхним диапазоном |
| `eps` | «Минимальное положительное число» | Расстояние от `1.0` до следующего представимого числа данного float dtype |

## Дополнительное чтение

- [NumPy: Data types](https://numpy.org/doc/stable/user/basics.types.html) — официальный обзор встроенных типов, переполнения и различий с числами Python.
- [numpy.iinfo](https://numpy.org/doc/stable/reference/generated/numpy.iinfo.html) — точные границы и число бит целочисленных dtype.
- [numpy.finfo](https://numpy.org/doc/stable/reference/generated/numpy.finfo.html) — диапазон, `eps`, normal и subnormal значения floating dtype.
- [NEP 50: Promotion rules](https://numpy.org/neps/nep-0050-scalar-promotion.html) — первичный документ о современных правилах продвижения типов в NumPy 2.x.
