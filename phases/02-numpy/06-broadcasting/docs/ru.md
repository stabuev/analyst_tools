# Broadcasting без магии

> Broadcasting сравнивает формы справа налево: длины совместимы, если равны или одна из них равна `1`.

**Тип:** Build
**Треки:** Core
**Пререквизит:** `02-numpy/05-aggregations`
**Время:** ~60 минут
**Результат:** проверяет совместимость форм и использует broadcasting осознанно.

## Цели обучения

- вручную вывести результирующую broadcast-форму;
- отличить совместимость формы от правильности смысла;
- нормализовать столбцы матрицы без Python-циклов;
- обнаружить нулевой масштаб и неверно ориентированный параметр;
- оценить риск большого промежуточного массива.

## Проблема

В прошлом уроке мы научились сворачивать ось наблюдений. Для матрицы признаков формы
`(100, 3)` среднее каждого столбца имеет форму `(3,)`. Теперь нужно применить эти три
агрегата обратно к ста строкам.

```python
normalized = (features - means) / scales
```

NumPy не создаёт вручную сто копий `means`. Он сопоставляет формы и применяет значения
вектора к каждой строке.

Но совместимая форма может иметь неверный смысл. Вектор среднего по строкам формы `(100,)`
не совместим с `(100, 3)`, а форма `(100, 1)` уже совместима, хотя нормализует другую
семантическую ось.

## Концепция

### Broadcasting расширяет поэлементную операцию

Самый простой случай — два массива одинаковой формы. Сложение, вычитание, умножение и
сравнение сопоставляют элементы на одинаковых позициях:

```python
import numpy as np

left = np.array([1.0, 2.0, 3.0])
right = np.array([10.0, 20.0, 30.0])

np.testing.assert_array_equal(left + right, [11.0, 22.0, 33.0])
```

Операция со скаляром уже является простейшим broadcasting: одно значение применяется к
каждой позиции массива.

```python
np.testing.assert_array_equal(left * 2, [2.0, 4.0, 6.0])
```

Операторы `+`, `-`, `*`, `/`, сравнения и многие универсальные функции NumPy выполняют
поэлементную операцию и используют правила broadcasting для согласования форм. Это не
универсальное правило любой функции. В частности, `*` означает поэлементное умножение, а
`@` — матричное умножение с отдельным контрактом осей.

### Формы выравниваются справа налево

Формы выравниваются по правому краю:

```text
matrix: (100, 3)
vector:      (3,)
result: (100, 3)
```

Для каждой пары осей допустимо:

1. длины равны;
2. одна длина равна `1`;
3. ось отсутствует и считается равной `1`.

Несовместимый пример:

```text
matrix: (100, 3)
vector:    (100,)
```

Последние длины `3` и `100` конфликтуют.

Совместимый, но другой по смыслу пример:

```text
matrix: (100, 3)
column: (100, 1)
result: (100, 3)
```

Здесь одно значение применяется ко всем трём столбцам каждой строки.

## Соберите это

### Ручное объединение двух форм

```python
def broadcast_shape(left, right):
    width = max(len(left), len(right))
    left = (1,) * (width - len(left)) + left
    right = (1,) * (width - len(right)) + right

    result = []
    for a, b in zip(left, right):
        if a == b or a == 1 or b == 1:
            result.append(max(a, b))
        else:
            raise ValueError("incompatible shapes")
    return tuple(result)
```

```python
assert broadcast_shape((5, 1), (1, 6)) == (5, 6)
assert broadcast_shape((100, 3), (3,)) == (100, 3)
```

Алгоритм не растягивает данные. Он только предсказывает логическую форму операции.

### Нормализация столбцов

Для каждого признака:

```text
z = (x - center) / scale
```

Ручная версия с циклами:

```python
result = []
for row in features:
    normalized_row = []
    for value, center, scale in zip(row, centers, scales):
        normalized_row.append((value - center) / scale)
    result.append(normalized_row)
```

Форма `centers` и `scales` должна соответствовать числу признаков, а масштаб не должен
быть нулевым.

## Используйте это

```python
import numpy as np

features = np.array([
    [1.0, 10.0],
    [3.0, 14.0],
    [5.0, 18.0],
])

centers = features.mean(axis=0)
scales = features.std(axis=0)
normalized = (features - centers) / scales
```

Формы:

```text
features:   (3, 2)
centers:       (2,)
scales:        (2,)
normalized: (3, 2)
```

Проверим результат:

```python
np.testing.assert_allclose(normalized.mean(axis=0), [0, 0], atol=1e-12)
np.testing.assert_allclose(normalized.std(axis=0), [1, 1], atol=1e-12)
```

Официальный предиктор NumPy:

```python
assert np.broadcast_shapes((3, 2), (2,), (2,)) == (3, 2)
```

Запуск артефакта:

```bash
uv run --locked python phases/02-numpy/06-broadcasting/outputs/feature_normalization.py \
  --matrix '[[1, 10], [3, 14], [5, 18]]'
```

Он возвращает параметры, нормализованную матрицу и контрольные средние со стандартными
отклонениями.

## Сломайте это

### Несовместимые формы

```python
np.zeros((4, 3)) + np.zeros(4)
```

Последние оси имеют длины `3` и `4`, поэтому операция отклоняется.

### Неверная семантика

Форма `(3, 1)` совместима с матрицей `(3, 2)`, но означает параметр на строку. Артефакт
нормализации принимает только `(2,)` или `(1, 2)`, потому что его контракт задаёт
параметры на столбец.

### Постоянный признак

Если стандартное отклонение равно нулю:

```text
(x - mean) / 0
```

нормализация не определена. Артефакт сообщает ошибку вместо выдачи `NaN` или `inf`.

### Большой промежуточный результат

Broadcasting обычно не копирует малый операнд, но сама операция может создать огромный
результат. Вычитание массивов форм `(50_000, 1, 100)` и `(1, 50_000, 100)` логически
создаёт форму `(50_000, 50_000, 100)`. Корректная форма не означает приемлемую память.

## Проверьте это

```bash
uv run --locked python -m unittest discover \
  -s phases/02-numpy/06-broadcasting/tests \
  -v
```

Тесты сравнивают ручной предиктор с `np.broadcast_shapes`, проверяют нормализацию,
ориентацию параметров и постоянные признаки.

```bash
uv run --locked python phases/02-numpy/06-broadcasting/code/main.py
```

## Поставьте результат

`outputs/feature_normalization.py` содержит:

- `broadcast_shape` без использования NumPy broadcasting;
- проверку параметров на семантическую ось признаков;
- `standardize_features`;
- JSON CLI с контрольными статистиками.

Функция возвращает нормализованные значения, центры и масштабы, поэтому те же параметры
можно применить к новым наблюдениям:

```python
train_scaled, centers, scales = standardize_features(train)
test_scaled, _, _ = standardize_features(test, center=centers, scale=scales)
```

## Упражнения

1. Выведите результат форм `(8, 1, 6, 1)` и `(7, 1, 5)` вручную.
2. Добавьте режим min-max нормализации с тем же shape-контрактом.
3. Оцените число элементов и память промежуточного результата попарных расстояний до его
   создания; отклоняйте расчёт выше заданного лимита.

## Ключевые термины

| Термин | Распространенное заблуждение | Точное значение |
|---|---|---|
| Broadcasting | «Физическое копирование малого массива» | Правила виртуального согласования форм |
| Trailing axis | «Первая ось» | Ось в правой части shape |
| Singleton axis | «Пустая ось» | Ось длины `1`, которую можно расширить |
| Совместимость | «Операция верна по смыслу» | Только возможность согласовать формы |
| Стандартизация | «Любое деление массива» | Вычитание центра и деление на ненулевой масштаб по выбранной оси |

## Дополнительное чтение

- [NumPy: Broadcasting](https://numpy.org/doc/stable/user/basics.broadcasting.html) — официальные правила, схемы совместимых форм и предупреждение о памяти.
- [numpy.broadcast_shapes](https://numpy.org/doc/stable/reference/generated/numpy.broadcast_shapes.html) — API для предсказания общей формы без выполнения арифметики.
- [numpy.broadcast_to](https://numpy.org/doc/stable/reference/generated/numpy.broadcast_to.html) — явное представление массива в новой broadcast-форме и его read-only view.
- [Array programming with NumPy](https://www.nature.com/articles/s41586-020-2649-2) — контекст array programming и векторных операций в экосистеме NumPy.
