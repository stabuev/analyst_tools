# Индексация, срезы и маски

> До выполнения индексации назовите смысл выбранных осей, предскажите `shape` результата и решите, должен ли он разделять память с источником.

**Тип:** Build
**Треки:** Core
**Пререквизит:** `02-numpy/03-dtypes`
**Время:** ~75 минут
**Результат:** по смыслу осей выбирает элементы, строки и столбцы, предсказывает `shape` результата и явно контролирует изменение источника или независимой копии.

## Цели обучения

- читать положительные и отрицательные индексы, а также срезы `start:stop:step`;
- выбирать данные по нескольким осям и предсказывать форму результата;
- понимать, когда целочисленный индекс удаляет ось, а срез сохраняет ее;
- строить маски из нескольких условий с `&`, `|`, `~` и явной политикой нечисловых значений;
- различать маску строк, маску столбцов и маску отдельных элементов;
- отличать basic indexing от advanced indexing и проверять совместное использование памяти;
- различать чтение через индекс и присваивание в индексируемую область;
- делать выбор между изменением исходного массива и работой с независимой копией явным.

## Проблема

После прошлых уроков мы умеем описать массив через предметные оси, проверить `shape` и
выбрать подходящий `dtype`. Теперь нужно работать не со всем массивом, а только с нужными
наблюдениями и признаками.

Представим маленькую числовую выгрузку заказов:

```python
import numpy as np

orders = np.array(
    [
        [101, 1, 1200, 2],
        [102, 3, 4500, 7],
        [103, 2, np.nan, 4],
        [104, 2, 3200, 3],
        [105, 5, 8000, 1],
    ],
    dtype=np.float64,
)

assert orders.shape == (5, 4)
```

Предметный контракт осей:

```text
axis 0: observation = order
axis 1: feature

feature 0: order_id
feature 1: item_count
feature 2: amount
feature 3: delivery_days
```

`order_id` здесь тоже хранится числом только ради компактного учебного примера. В реальной
таблице идентификатор, сумма и срок доставки имеют разные семантические типы, поэтому
после знакомства с pandas такой набор удобнее хранить в `DataFrame`.

Задача аналитика:

1. оставить заказы с конечной суммой от 1000 до 5000 включительно;
2. оставить среди них заказы, доставленные не более чем за 5 дней;
3. вернуть только `order_id`, `amount` и `delivery_days`;
4. не изменить исходный массив;
5. получить результат формы `(selected_order, selected_feature)`.

Здесь легко допустить несколько разных ошибок.

```python
sample = orders[:2]
sample[:, 2] = 0
```

`sample` выглядит как отдельный массив, но базовый срез обычно является view. Изменение
попадет и в `orders`.

Другой пример:

```python
amount = orders[:, 2]
```

Значения выбраны правильно, но ось признаков исчезла: форма стала `(5,)`, а не `(5, 1)`.
Следующая функция может ожидать матрицу наблюдений и признаков и отклонить такой результат.

Поэтому индексация отвечает сразу на три вопроса:

1. **Какие значения выбраны?**
2. **Какие оси остались и каков `shape`?**
3. **Разделяет ли результат память с источником?**

Ответ только на первый вопрос недостаточен.

## Концепция

### Индекс — это координата на предметной оси

В одномерном массиве одна позиция индекса относится к единственной оси:

```python
values = np.array([10, 20, 30, 40])

assert values[0] == 10
assert values[2] == 30
assert values[-1] == 40
assert values[-2] == 30
```

Нумерация слева начинается с `0`. Отрицательные индексы считаются справа: `-1` — последний
элемент, `-2` — предпоследний.

Индекс должен попадать в длину оси:

```python
values[4]   # IndexError: ось имеет длину 4
values[-5]  # IndexError по той же причине
```

Число в квадратных скобках — не идентификатор наблюдения. `values[2]` означает третью
позицию массива, а не строку с бизнес-ключом `2`. NumPy использует позиционную модель.

### Срез описывает полуинтервал позиций

Синтаксис среза:

```text
start:stop:step
```

Левая граница включается, правая не включается:

```python
values = np.array([10, 20, 30, 40, 50])

np.testing.assert_array_equal(values[1:4], [20, 30, 40])
```

Такой полуинтервал удобен: длина простого среза `start:stop` равна `stop - start`, если обе
границы находятся внутри массива и шаг равен единице.

Пропущенная граница означает начало или конец:

```python
np.testing.assert_array_equal(values[:3], [10, 20, 30])
np.testing.assert_array_equal(values[2:], [30, 40, 50])
np.testing.assert_array_equal(values[:], values)
```

Шаг выбирает каждую `step`-ю позицию:

```python
np.testing.assert_array_equal(values[::2], [10, 30, 50])
np.testing.assert_array_equal(values[::-1], [50, 40, 30, 20, 10])
```

Срез обычно не сообщает об ошибке, если граница вышла за длину массива:

```python
np.testing.assert_array_equal(values[:100], values)
```

Это удобно, но способно скрыть неверное ожидание размера. Если алгоритму нужны ровно три
наблюдения, проверьте форму результата отдельно.

### В многомерном массиве каждая позиция относится к своей оси

Для матрицы `(observation, feature)` первый индекс выбирает наблюдения, второй — признаки:

```python
assert orders[0, 2] == 1200
```

Читаем слева направо:

```text
orders[0, 2]
       │  └── feature 2: amount
       └───── observation 0: первый заказ
```

Двоеточие означает «вся ось»:

```python
first_order = orders[0, :]
all_amounts = orders[:, 2]
```

У `first_order` форма `(4,)`, у `all_amounts` — `(5,)`.

### Целочисленный индекс удаляет ось, срез сохраняет

Это главное правило предсказания формы в базовой индексации.

| Выражение | Предметный смысл | `shape` |
|---|---|---:|
| `orders[1, 2]` | одно значение одного признака | `()` |
| `orders[1]` | все признаки одного заказа | `(4,)` |
| `orders[1:2]` | один заказ как матрица | `(1, 4)` |
| `orders[:, 2]` | сумма каждого заказа как вектор | `(5,)` |
| `orders[:, 2:3]` | сумма каждого заказа как матрица с одним признаком | `(5, 1)` |
| `orders[:3, 1:4]` | три заказа и три признака | `(3, 3)` |

Проверим различие кодом:

```python
assert orders[1].shape == (4,)
assert orders[1:2].shape == (1, 4)

assert orders[:, 2].shape == (5,)
assert orders[:, 2:3].shape == (5, 1)
```

Значения могут выглядеть одинаково при печати, но контракт для следующей операции разный.
Если требуется сохранить ось, используйте срез длины один, а не целое число.

### `np.newaxis` добавляет ось длины один

Предыдущий урок вводил `np.expand_dims`. Внутри квадратных скобок та же операция часто
записывается через `np.newaxis`:

```python
amount_vector = orders[:, 2]
amount_column = amount_vector[:, np.newaxis]

assert amount_vector.shape == (5,)
assert amount_column.shape == (5, 1)
```

`np.newaxis` является читаемым псевдонимом для `None`:

```python
same_column = amount_vector[:, None]
assert same_column.shape == (5, 1)
```

В учебных материалах будем предпочитать `np.newaxis`, потому что оно явно называет
намерение.

Многоточие `...` подставляет столько полных срезов `:`, сколько нужно для оставшихся осей:

```python
daily_metrics = np.zeros((2, 3, 2))
with_scenario = daily_metrics[..., np.newaxis]

assert with_scenario.shape == (2, 3, 2, 1)
```

Это продолжает пример `(store, day, metric)` прошлого урока: все существующие оси
сохраняются, а справа появляется `scenario` длины один.

### Basic indexing обычно возвращает view

К basic indexing относятся целые индексы, срезы, `...` и `np.newaxis`. Когда результат
остается массивом, он обычно обращается к тому же буферу данных:

```python
source = np.arange(12).reshape(3, 4)
part = source[1:3, :2]

assert np.shares_memory(source, part)
```

`part` имеет свои `shape` и `strides`, но его значения лежат в буфере `source`.

```python
part[0, 0] = 999
assert source[1, 0] == 999
```

Такой результат называется **view**. Он экономит память и позволяет быстро работать с
частью большого массива. Проблема возникает не в самом view, а в неявном предположении,
что перед нами независимая копия.

Есть и обратная сторона экономии: маленький долгоживущий view продолжает ссылаться на
буфер большого источника. Если из большого временного массива нужна только небольшая
часть, которую предстоит хранить долго, явная `.copy()` может освободить исходный буфер
раньше. Проверяйте отношение через `np.shares_memory`; поле `.base` является деталью
цепочки представлений, а не удобным контрактом независимости.

### Advanced indexing возвращает копию при чтении

Массив целых индексов выбирает позиции в указанном порядке:

```python
source = np.array([10, 20, 30, 40])
selected = source[[3, 1, 1]]

np.testing.assert_array_equal(selected, [40, 20, 20])
assert not np.shares_memory(source, selected)
```

Позиции можно переставлять и повторять. Для результата приходится собирать новый буфер,
поэтому такое чтение относится к advanced indexing и возвращает копию.

Булева маска тоже относится к advanced indexing:

```python
mask = np.array([False, True, True, False])
selected = source[mask]

np.testing.assert_array_equal(selected, [20, 30])
assert not np.shares_memory(source, selected)
```

Минимальная рабочая модель:

| Способ чтения | Пример | Обычный результат |
|---|---|---|
| Basic indexing | `x[1:4]`, `x[:, 2]` | view, если результат остается массивом |
| Integer advanced indexing | `x[[0, 3]]` | копия |
| Boolean advanced indexing | `x[x > 0]` | копия |

Слово «обычный» важно для basic indexing: не пытайтесь угадывать память по внешнему виду.
Если независимость существенна, вызовите `.copy()` или проверьте `np.shares_memory`.

### Сравнение строит булев массив той же формы

```python
amounts = orders[:, 2]
amount_ok = amounts >= 1000

assert amount_ok.dtype == np.dtype("bool")
assert amount_ok.shape == amounts.shape == (5,)
```

Каждое значение маски относится к позиции с тем же номером:

```text
amounts:   [1200, 4500, NaN, 3200, 8000]
amount_ok: [True, True, False, True, True]
```

Сравнение с `NaN` дает `False`, но не стоит строить на этом неявную политику. В числовом
фильтре лучше прямо назвать требование конечности:

```python
amount_is_finite = np.isfinite(amounts)
```

`np.isfinite` исключает и `NaN`, и положительную или отрицательную бесконечность.

### Условия объединяются поэлементно

Для массивов используются:

- `&` — поэлементное «И»;
- `|` — поэлементное «ИЛИ»;
- `~` — поэлементное «НЕ».

Например:

```python
invalid_amount = ~np.isfinite(amounts)
suspicious_amount = (amounts < 0) | (amounts > 100_000)
```

Каждое сравнение заключайте в скобки:

```python
amount_ok = (
    np.isfinite(amounts)
    & (amounts >= 1000)
    & (amounts <= 5000)
)
```

Условие на другой признак:

```python
delivery_days = orders[:, 3]
delivered_quickly = np.isfinite(delivery_days) & (delivery_days <= 5)

row_mask = amount_ok & delivered_quickly
```

Операторы Python `and`, `or` и `not` ожидают одно логическое значение. У массива значений
несколько, поэтому `amount_ok and delivered_quickly` приводит к ошибке о неоднозначном
truth value.

### Форма маски определяет, что она выбирает

Для матрицы формы `(5, 4)` встречаются три разных контракта.

**Маска строк** имеет форму `(5,)` и применяется к первой оси:

```python
row_mask = np.array([True, False, False, True, False])
selected_rows = orders[row_mask]

assert selected_rows.shape == (2, 4)
```

**Маска столбцов** имеет форму `(4,)` и явно ставится на ось признаков:

```python
column_mask = np.array([True, False, True, True])
selected_columns = orders[:, column_mask]

assert selected_columns.shape == (5, 3)
```

**Маска элементов** имеет ту же форму `(5, 4)`, что и матрица. При чтении она собирает все
позиции `True` в один одномерный результат:

```python
element_mask = orders > 3000
large_values = orders[element_mask]

assert large_values.ndim == 1
assert large_values.size == int(element_mask.sum())
```

Это не «фильтрация строк». Предметная структура осей в таком результате потеряна.

Маска формы `(5, 1)` не становится маской строк автоматически. Boolean indexing не обязан
транслировать маску так, как арифметические операции используют broadcasting. Для выбора
строк нужен контракт ровно `(5,)`.

### Чтение и присваивание — разные операции

При чтении advanced indexing создает копию:

```python
source = np.array([10, 20, 30, 40])
mask = source >= 30
selected = source[mask]

selected[:] = 0
np.testing.assert_array_equal(source, [10, 20, 30, 40])
```

Но индексированное присваивание изменяет позиции самого объекта слева:

```python
source[mask] = 0
np.testing.assert_array_equal(source, [10, 20, 0, 0])
```

NumPy не обязан сначала возвращать временную копию `source[mask]`. Вся конструкция
`source[mask] = 0` означает «записать значение в выбранные позиции `source`».

Отсюда следует опасность цепочки:

```python
matrix = np.arange(12).reshape(3, 4)
matrix[[0, 2]][:, 1] = -1
```

Первое чтение `matrix[[0, 2]]` создает временную копию. Изменится эта копия, а `matrix`
останется прежней. Для изменения источника сформируйте один индекс или явную маску и
присвойте через него:

```python
matrix[[0, 2], 1] = -1
```

### Два advanced-индекса могут выбирать пары, а не прямоугольник

```python
matrix = np.arange(12).reshape(3, 4)
row_indices = np.array([0, 2])
column_indices = np.array([1, 3])

pairs = matrix[row_indices, column_indices]
```

`pairs` содержит координаты `(0, 1)` и `(2, 3)`, поэтому его форма `(2,)`. Это не две
строки на два столбца.

Для прямоугольника используйте явный декартов выбор:

```python
rectangle = matrix[np.ix_(row_indices, column_indices)]
assert rectangle.shape == (2, 2)
```

Общие правила совместного advanced indexing значительно шире. Для аналитического
минимума достаточно помнить этот failure mode, использовать `np.ix_` для прямоугольника
или делить сложную выборку на два именованных шага.

### Контракт выборки состоит из семи решений

Перед рабочей индексацией ответьте:

1. Что означает каждая ось источника?
2. Какие позиции или условия выбирают каждую ось?
3. Каков ожидаемый `shape` результата?
4. Должна ли сохраниться ось длины один?
5. Что делать с `NaN` и бесконечностями?
6. Нужна ли связь с буфером источника или независимая копия?
7. Если значения изменяются, должен ли измениться источник и подходит ли replacement его `dtype`?

Такой контракт соединяет три предыдущих урока: смысл осей, форму и тип данных.

## Соберите это

Сначала реализуем прозрачный механизм выбора без NumPy. Он покажет, что маска строк — это
одно решение `True` или `False` на каждое наблюдение, а список столбцов — позиционная
проекция признаков.

### Шаг 1. Зафиксируйте строки, маску и столбцы

```python
rows = [
    [101, 1, 1200, 2],
    [102, 3, 4500, 7],
    [103, 2, None, 4],
    [104, 2, 3200, 3],
    [105, 5, 8000, 1],
]

amount_ok = [True, True, False, True, False]
delivered_quickly = [True, False, True, True, True]
row_mask = [
    amount and delivery
    for amount, delivery in zip(amount_ok, delivered_quickly, strict=True)
]

column_indices = [0, 2, 3]
```

Проверьте контракт до выборки:

```python
assert row_mask == [True, False, False, True, False]
assert sum(row_mask) == 2
assert len(column_indices) == 3
```

Ожидаемая форма результата — `(2, 3)`.

### Шаг 2. Выберите строки и столбцы вручную

```python
def select_rows_and_columns(rows, row_mask, column_indices):
    if not rows:
        raise ValueError("rows must not be empty")

    width = len(rows[0])
    if width == 0 or any(len(row) != width for row in rows):
        raise ValueError("rows must form a non-empty rectangle")
    if len(row_mask) != len(rows):
        raise ValueError("row mask must contain one decision per row")
    if any(type(keep) is not bool for keep in row_mask):
        raise TypeError("row mask must contain booleans")
    if not column_indices:
        raise ValueError("at least one column is required")
    if any(column < 0 or column >= width for column in column_indices):
        raise IndexError("column index is out of bounds")

    selected = []
    for row, keep in zip(rows, row_mask, strict=True):
        if keep:
            selected.append([row[column] for column in column_indices])
    return selected
```

```python
manual_result = select_rows_and_columns(rows, row_mask, column_indices)

assert manual_result == [
    [101, 1200, 2],
    [104, 3200, 3],
]
```

Функция не вычисляет аналитические условия. Она делает видимым механизм:

- `row_mask[i]` принимает или отклоняет строку `rows[i]`;
- порядок выбранных строк сохраняется;
- каждый результат содержит столбцы в порядке `column_indices`;
- создаются новые внутренние списки, поэтому источник не изменяется.

### Шаг 3. Проверьте инварианты результата

```python
assert len(manual_result) == sum(row_mask)
assert all(len(row) == len(column_indices) for row in manual_result)
assert rows[0] == [101, 1, 1200, 2]
```

В NumPy цикл выбора скрыт внутри advanced indexing, но предметные инварианты остаются
теми же.

## Используйте это

### Постройте маски отдельно и назовите их

```python
import numpy as np

ORDER_ID = 0
ITEM_COUNT = 1
AMOUNT = 2
DELIVERY_DAYS = 3

orders = np.array(
    [
        [101, 1, 1200, 2],
        [102, 3, 4500, 7],
        [103, 2, np.nan, 4],
        [104, 2, 3200, 3],
        [105, 5, 8000, 1],
    ],
    dtype=np.float64,
)

amounts = orders[:, AMOUNT]
delivery_days = orders[:, DELIVERY_DAYS]

amount_ok = (
    np.isfinite(amounts)
    & (amounts >= 1000)
    & (amounts <= 5000)
)
delivered_quickly = np.isfinite(delivery_days) & (delivery_days <= 5)
row_mask = amount_ok & delivered_quickly
```

Именованные условия легче проверить по отдельности, чем одно длинное выражение:

```python
np.testing.assert_array_equal(
    amount_ok,
    [True, True, False, True, False],
)
np.testing.assert_array_equal(
    delivered_quickly,
    [True, False, True, True, True],
)
np.testing.assert_array_equal(
    row_mask,
    [True, False, False, True, False],
)
```

### Выберите строки и столбцы в два понятных этапа

```python
selected_rows = orders[row_mask]
selected = selected_rows[:, [ORDER_ID, AMOUNT, DELIVERY_DAYS]]

assert selected.shape == (2, 3)
assert selected.dtype == orders.dtype
np.testing.assert_array_equal(
    selected,
    [
        [101, 1200, 2],
        [104, 3200, 3],
    ],
)
```

Первый этап выбирает наблюдения, второй — признаки. Оба используют advanced indexing и
возвращают независимые буферы:

```python
assert not np.shares_memory(orders, selected_rows)
assert not np.shares_memory(orders, selected)
```

Выборка не создает отдельный `dtype` для каждого столбца: весь результат сохраняет общий
тип исходного `ndarray`. Поэтому `order_id` в этом примере остается `float64` вместе с
суммой и сроком доставки — еще одна причина перейти к табличной структуре для смешанных
семантических типов.

Два этапа здесь немного длиннее, зато смысл каждой оси и ожидаемая форма остаются видимыми.

### Сохраните ось признаков, если следующему шагу нужна матрица

```python
amount_vector = orders[:, AMOUNT]
amount_matrix = orders[:, AMOUNT:AMOUNT + 1]

assert amount_vector.shape == (5,)
assert amount_matrix.shape == (5, 1)
```

Оба результата содержат одну сумму на заказ. Выбор зависит не от значений, а от контракта
следующей функции.

### Изменяйте источник только явно

```python
scores = np.array([12.0, -3.0, 18.0])
invalid = scores < 0

cleaned = scores.copy()
cleaned[invalid] = np.nan

np.testing.assert_array_equal(scores, [12.0, -3.0, 18.0])
np.testing.assert_array_equal(cleaned, [12.0, np.nan, 18.0])
```

Если изменение источника действительно является частью контракта, оно должно быть видно
в имени операции, параметре или документации:

```python
scores[invalid] = np.nan
np.testing.assert_array_equal(scores, [12.0, np.nan, 18.0])
```

Замена пропуска на ноль, отрицательного значения на `NaN` или выброса на границу — это
предметное решение, а не свойство NumPy. Урок показывает механизм изменения, но политика
должна следовать из смысла данных.

### Проверьте контракт переиспользуемым артефактом

Артефакт урока содержит четыре основные операции:

- `build_range_mask` строит одномерную маску диапазона и явно исключает `NaN` и бесконечности;
- `select_rows` проверяет маску строк и столбцы, сохраняет порядок и возвращает независимый массив;
- `replace_where` изменяет копию по умолчанию и требует `in_place=True` для изменения исходного ndarray;
- `memory_report` показывает форму и память для среза, advanced selection, вектора и матрицы одного признака.

Полный пример запускается так:

```bash
uv run --locked python phases/02-numpy/04-indexing-and-masks/code/main.py
```

CLI-аудит одной выборки:

```bash
uv run --locked python phases/02-numpy/04-indexing-and-masks/outputs/selection_contract.py \
  --matrix '[[101, 1, 1200, 2], [102, 3, 4500, 7], [103, 2, null, 4], [104, 2, 3200, 3], [105, 5, 8000, 1]]' \
  --filter-column 2 \
  --lower 1000 \
  --upper 5000 \
  --columns '[0, 2, 3]'
```

Здесь JSON `null` преобразуется в `NaN`. Отчет показывает:

```text
axis_contract: [observation, feature]
source_shape: [5, 4]
filter_column: 2
mask_shape: [5]
selected_count: 3
excluded_non_finite: 1
selected_shape: [3, 3]
shares_memory: false
```

CLI демонстрирует один диапазон по одному столбцу. Для составного предметного условия,
как в основном примере, объединяйте несколько результатов `build_range_mask` и сравнений,
а затем передавайте итог в `select_rows`.

### Где заканчивается применимость артефакта

Артефакт намеренно работает только с непустой двумерной числовой матрицей. Он не хранит:

- имена столбцов;
- разные `dtype` для разных признаков;
- бизнес-ключ отдельно от позиции строки;
- причины пропусков;
- предметную политику исправления значений.

Для однородных числовых расчетов это полезное ограничение. Для обычной аналитической
таблицы с идентификаторами, категориями, датами и именованными столбцами после фазы NumPy
мы перейдем к pandas.

## Сломайте это

### Ошибка 1. Включить правую границу среза мысленно

```python
values = np.array([10, 20, 30, 40, 50])
part = values[1:4]
```

`part` содержит позиции `1`, `2`, `3`, но не `4`. Для ожидания ровно трех элементов
проверьте `part.shape == (3,)`.

### Ошибка 2. Случайно удалить ось

```python
one_feature = orders[:, 2]
assert one_feature.shape == (5,)
```

Если требуется матрица `(observation, feature)`, используйте `orders[:, 2:3]` или
`orders[:, [2]]`; оба дадут форму `(5, 1)`, но первый результат обычно view, а второй —
копия.

### Ошибка 3. Использовать `and` или `or` для массивов

```python
mask = (amounts >= 1000) and (amounts <= 5000)
```

Python пытается получить одно логическое значение всего массива. Используйте `&` или `|`
и скобки вокруг каждого сравнения.

### Ошибка 4. Убрать скобки вокруг сравнений

```python
mask = amounts >= 1000 & amounts <= 5000
```

Приоритет операторов не выражает требуемую цепочку поэлементных условий. Надежная запись:

```python
mask = (amounts >= 1000) & (amounts <= 5000)
```

### Ошибка 5. Передать маску формы `(row, 1)` вместо `(row,)`

```python
row_mask = np.array([[True], [False], [False], [True], [False]])
orders[row_mask]
```

Для выбора строк нужна одномерная маска формы `(5,)`. Не рассчитывайте на broadcasting в
boolean indexing.

### Ошибка 6. Принять маску элементов за фильтр строк

```python
positive = orders > 0
result = orders[positive]
```

`result` будет одномерным набором отдельных значений. Если нужно оставить строки по
условию на сумму, сначала постройте `orders[:, AMOUNT] > 0`, получите маску `(5,)` и
примените ее к первой оси.

### Ошибка 7. Скрыто изменить источник через view

```python
source = np.arange(12).reshape(3, 4)
part = source[:2]
part[:, 0] = -1

assert source[0, 0] == -1
```

Если изменение источника не требуется:

```python
part = source[:2].copy()
assert not np.shares_memory(source, part)
```

### Ошибка 8. Пытаться изменить источник через временную advanced-копию

```python
source = np.arange(12).reshape(3, 4)
source[[0, 2]][:, 1] = -1
```

`source` не изменится. Первая операция собрала временную копию. Используйте один индекс:

```python
source[[0, 2], 1] = -1
```

### Ошибка 9. Ожидать прямоугольник от двух массивов индексов

```python
result = source[[0, 2], [1, 3]]
assert result.shape == (2,)
```

Выбраны две пары координат. Для прямоугольника используйте:

```python
result = source[np.ix_([0, 2], [1, 3])]
assert result.shape == (2, 2)
```

### Ошибка 10. Не назвать политику `NaN` и бесконечностей

```python
mask = amounts <= 5000
```

`NaN` будет исключен как побочный эффект сравнения, но смысл останется скрытым. Запись
`np.isfinite(amounts) & (amounts <= 5000)` делает политику явной и исключает также
бесконечности.

### Ошибка 11. Забыть о `dtype` при присваивании

```python
counts = np.array([1, 2, 3], dtype=np.int16)
counts[counts < 2] = 1.5
```

Тип массива не меняется автоматически ради replacement. Возможна потеря дробной части,
ошибка или выход за диапазон в зависимости от операции и версии NumPy. Артефакт
`replace_where` использует `casting="same_value"` и отклоняет замену, которую нельзя
сохранить без изменения значения. Это прямое продолжение dtype-контракта прошлого урока.

## Проверьте это

Для любой важной выборки проверяйте не только значения.

```python
source_before = orders.copy()

assert row_mask.dtype == np.dtype("bool")
assert row_mask.shape == (orders.shape[0],)
assert selected.shape == (int(row_mask.sum()), 3)
assert not np.shares_memory(orders, selected)
np.testing.assert_array_equal(orders, source_before)
```

Полезные инварианты:

1. число решений маски равно длине выбранной оси;
2. число строк результата равно количеству `True`;
3. число столбцов результата соответствует контракту проекции;
4. порядок строк и столбцов предсказуем;
5. нечисловые значения обрабатываются по явной политике;
6. источник изменяется только в режиме, где это обещано;
7. replacement сохраняется в существующем `dtype` без неожиданного преобразования.

Behavioral tests урока:

```bash
uv run --locked python -m unittest discover \
  -s phases/02-numpy/04-indexing-and-masks/tests \
  -v
```

Они проверяют:

- открытые и закрытые границы диапазона;
- односторонние фильтры и исключение `NaN`/бесконечностей;
- точную форму и `bool`-тип маски строк;
- порядок и форму выбранных строк и столбцов;
- отрицательные индексы столбцов;
- независимость результата от источника;
- режимы copy и in-place при замене;
- совместимость replacement с `dtype`;
- различия формы и памяти basic и advanced indexing;
- JSON-отчет CLI и понятные ошибки без traceback.

## Поставьте результат

Именованный артефакт:

```text
outputs/selection_contract.py
```

Он поставляет:

- проверяемый конструктор маски диапазона;
- выбор строк и столбцов с явным shape-контрактом;
- независимый результат выборки;
- безопасный по умолчанию механизм изменения по маске;
- отчет о форме, числе выбранных строк, пропусках, `dtype` и общей памяти;
- standalone CLI с JSON-входом и JSON-выходом.

Сохранение отчета в файл:

```bash
uv run --locked python phases/02-numpy/04-indexing-and-masks/outputs/selection_contract.py \
  --lower 1000 \
  --upper 5000 \
  --output selection-report.json
```

Коды завершения:

- `0` — контракт выполнен, отчет построен;
- `2` — входной контракт неверен: форма, тип маски, индекс, границы или JSON.

Артефакт можно перенести в небольшой числовой пайплайн. `code/main.py` показывает импорт
файла, построение составной маски и проверку независимости результата.

## Упражнения

1. Для массива формы `(6, 4)` предскажите формы `x[2]`, `x[2:3]`, `x[:, -1]`,
   `x[:, -1:]`, `x[::2, 1:3]`, а затем подтвердите их assertions.
2. Добавьте к примеру условие `item_count >= 2`. Сначала сохраните его в отдельной маске,
   затем объедините с существующими условиями и предскажите число выбранных строк.
3. Используйте `build_range_mask` только с нижней, а затем только с верхней границей.
   Проверьте все четыре политики `inclusive` на значениях, равных границе.
4. Напишите функцию, которая принимает матрицу `(observation, feature)` и номер признака,
   но возвращает результат строго формы `(observation, 1)`. Покройте тестом ошибку
   случайного схлопывания оси.
5. Постройте срез большого массива, измените его и зафиксируйте побочный эффект тестом.
   Затем добавьте `.copy()` и докажите независимость через `np.shares_memory`. Объясните,
   почему маленький долгоживущий view может удерживать память большого источника.
6. Выберите строки `[0, 3]` и столбцы `[0, 2, 3]` как прямоугольник через `np.ix_`.
   Сравните форму и значения с парным индексом `x[[0, 3], [0, 2]]`.

## Ключевые термины

| Термин | Распространенное заблуждение | Точное значение |
|---|---|---|
| Позиционный индекс | «Бизнес-идентификатор строки» | Координата на оси ndarray, считающаяся от нуля или с конца |
| Срез | «Обе границы включаются» | Полуинтервал `start:stop:step`, в котором `stop` не включается |
| `:` | «Любые значения массива» | Сохранение всех позиций соответствующей оси |
| `np.newaxis` | «Создание новых значений» | Вставка оси длины один в результат индексации |
| Basic indexing | «Любой доступ через скобки» | Индексация целыми числами, срезами, `...` и newaxis; массив-результат обычно является view |
| Advanced indexing | «Более сложный, но такой же срез» | Выбор массивами целых индексов или булевыми масками; при чтении создается копия |
| Булева маска | «Одно значение True или False» | Массив позиционных решений для конкретной оси или всех элементов |
| Маска строк | «Любой bool-массив» | Одномерная маска формы `(row,)`, выбирающая позиции первой оси матрицы |
| View | «Независимый подмассив» | Объект массива, разделяющий область памяти с источником |
| Copy | «Еще одно имя того же массива» | Массив с независимым буфером данных |
| Индексированное присваивание | «Изменение временной копии» | Запись непосредственно в позиции объекта слева, например `x[mask] = value` |
| `np.shares_memory` | «Проверка равенства значений» | Точная проверка пересечения областей памяти двух массивов |
| `np.ix_` | «Обязательный синтаксис любого среза» | Способ построить прямоугольный выбор по нескольким одномерным наборам индексов |

## Дополнительное чтение

- [Яндекс Образование: модули `math` и `numpy` (RU)](https://education.yandex.ru/handbook/python/article/moduli-math-i-numpy) — для быстрого повторения прочитайте только раздел «Индексация, срезы и обход массива». На примерах двумерных массивов еще раз проследите, какой индекс относится к строкам, какой — к столбцам и как шаг среза меняет выбранные позиции; остальные разделы статьи для этого урока необязательны.
- [Дмитрий Макаров: массив NumPy (RU)](https://www.dmitrymakarov.ru/python/numpy/) — откройте раздел «Индексы и срезы»: схемы помогают связать координаты многомерного массива с позициями в `shape`, а примеры разбирают отрицательные индексы и шаг. После каждого выражения сначала предскажите форму результата и только затем смотрите вывод; широкие разделы о разреженных матрицах и линейной алгебре можно пропустить.
- [С. В. Лемешевский: основы NumPy — массивы и векторные вычисления (RU)](https://slemeshevsky.github.io/python-course/numpy/html/._numpy-flatly001.html) — сосредоточьтесь на разделах «Основы индексирования и срезы», «Индексирование с помощью срезов» и «Логическое (Boolean) индексирование». Они показывают связь маски строк с первой осью и побочные эффекты view; примеры написаны для более ранней версии NumPy, поэтому спорные детали сверяйте с актуальной официальной документацией ниже.
- [NumPy: How to index ndarrays (EN)](https://numpy.org/doc/stable/user/how-to-index.html) — прикладное продолжение урока. Разберите разделы про выбор строк и столбцов, сохранение размерности через `np.newaxis`, создание подматриц, фильтрацию и замену значений; после каждого примера фиксируйте не только значения, но и ожидаемые `shape` и отношение к источнику.
- [SciPy Lecture Notes: The NumPy array object (EN)](https://scipy-lectures.org/intro/numpy/array_object.html) — прочитайте только «Indexing and slicing», «Copies and views» и «Fancy indexing», затем выполните упражнения рядом с этими разделами. Материал полезен как второй связный разбор одной темы, но точный современный контракт проверяйте по документации NumPy 2.x.
- [NumPy: Indexing on ndarrays (EN)](https://numpy.org/doc/stable/user/basics.indexing.html) — главный официальный справочник по basic, integer advanced и boolean indexing, `np.newaxis` и индексированному присваиванию. Для этого курса обязательны разделы Basic indexing, Advanced indexing, Boolean array indexing и Assigning values; field access и полную систему broadcasting нескольких advanced-индексов пока можно пропустить.
- [NumPy: Copies and views (EN)](https://numpy.org/doc/stable/user/basics.copies.html) — точная модель буфера и metadata массива. Воспроизведите примеры из раздела Indexing operations и объясните своими словами, почему basic slicing создает view, advanced indexing — копию, а явная `.copy()` иногда нужна не только для защиты от изменения, но и для освобождения большого исходного буфера.
- [`numpy.shares_memory` (EN)](https://numpy.org/doc/stable/reference/generated/numpy.shares_memory.html) — API-контракт точной проверки пересечения областей памяти. Сначала повторите короткие примеры с общим и независимым буфером; замечание о сложности и параметре `max_work` нужно помнить для необычных массивов со сложными strides, но подробно разбирать его в этом уроке не требуется.
- [`numpy.isfinite` (EN)](https://numpy.org/doc/stable/reference/generated/numpy.isfinite.html) — справочник по поэлементной проверке `NaN`, `+inf` и `-inf`. Проверьте, что функция возвращает булев результат той же формы, затем объедините его через `&` с предметным условием диапазона и сравните с неявным исключением `NaN` обычным сравнением.
- [`numpy.ix_` (EN)](https://numpy.org/doc/stable/reference/generated/numpy.ix_.html) — точный ответ на failure mode с двумя массивами индексов. Повторите пример открытой сетки, сравните `x[rows, columns]` с `x[np.ix_(rows, columns)]` и объясните, почему первый вариант выбирает пары координат, а второй — их декартово произведение.
