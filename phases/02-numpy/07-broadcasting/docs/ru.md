# Broadcasting без магии

> Broadcasting сопоставляет позиции осей, а не их смысл: сначала докажите совместимость форм, затем отдельно проверьте предметные оси, `dtype` и размер результата.

**Тип:** Build
**Треки:** Core
**Пререквизит:** `02-numpy/06-aggregations`
**Время:** ~90 минут
**Результат:** по предметным осям и `dtype` операндов предсказывает результат поэлементной broadcast-операции, явно добавляет единичные оси и проверяет семантику, тип, запись на месте и память.

## Цели обучения

- объяснить поэлементную операцию как сопоставление координат и отличить ее от редукции и матричного умножения;
- различать скаляр `()`, вектор `(n,)`, строку `(1, n)` и столбец `(n, 1)`;
- вручную вывести общую форму двух или нескольких операндов справа налево;
- показать, какая координата малого операнда используется для каждого элемента результата;
- применять `np.newaxis`, `reshape` и `keepdims` для явного расположения параметра на предметной оси;
- отдельно проверять совместимость форм, смысл осей, допустимость операции для `dtype` и тип результата;
- отличать логическое расширение от физической копии и заранее оценивать память результата;
- распознавать семантически неверные, in-place несовместимые и слишком большие операции.

## Проблема

В предыдущем уроке мы рассчитали среднее и стандартное отклонение каждого показателя в
массиве ежедневных метрик:

```python
import numpy as np

daily_metrics = np.array(
    [
        [[10.0, 1000.0], [12.0, 1440.0], [8.0, 880.0]],
        [[7.0, 840.0], [9.0, 990.0], [11.0, 1320.0]],
    ]
)

assert daily_metrics.shape == (2, 3, 2)
```

Контракт осей:

```text
(store=2, day=3, metric=2)
```

Агрегаты сохраняют позиции свернутых осей:

```python
metric_means = daily_metrics.mean(axis=(0, 1), keepdims=True)
metric_stds = daily_metrics.std(axis=(0, 1), keepdims=True)

assert metric_means.shape == (1, 1, 2)
assert metric_stds.shape == (1, 1, 2)
```

Их контракт:

```text
(summarized_store=1, summarized_day=1, metric=2)
```

Теперь нужно применить два параметра каждого показателя ко всем магазинам и дням:

```python
standardized = (daily_metrics - metric_means) / metric_stds
```

NumPy принимает эту запись, хотя формы различаются. Он не создает руками шесть копий
каждого среднего. Вместо этого NumPy сопоставляет оси и для каждой координаты результата
решает, какой элемент каждого операнда использовать.

Но у автоматизма есть границы:

- совместимые длины могут относиться к разным предметным осям;
- одномерный вектор не сообщает, является он строкой или столбцом;
- одна и та же форма может дать разный `dtype` для сложения, деления и сравнения;
- маленький операнд не требует копии, но результат операции может оказаться огромным;
- операция вне выражения может работать, а запись результата обратно через `+=` — нет.

Поэтому broadcasting — не трюк для устранения циклов, а контракт из нескольких
независимых вопросов.

## Концепция

### Сначала поэлементная операция одинаковых форм

Рассмотрим два массива одной формы:

```python
left = np.array([[1, 2], [3, 4]])
right = np.array([[10, 20], [30, 40]])

result = left + right
```

Каждая координата результата использует ту же координату обоих операндов:

```text
result[0, 0] = left[0, 0] + right[0, 0]
result[0, 1] = left[0, 1] + right[0, 1]
result[1, 0] = left[1, 0] + right[1, 0]
result[1, 1] = left[1, 1] + right[1, 1]
```

```python
np.testing.assert_array_equal(result, [[11, 22], [33, 44]])
```

Так работают `+`, `-`, `*`, `/`, сравнения и многие функции NumPy. Они применяют одну
маленькую операцию к соответствующим элементам всего массива.

### Что такое ufunc на нужном нам уровне

`np.add`, `np.subtract`, `np.multiply`, `np.true_divide`, `np.maximum` и `np.equal` —
универсальные функции, или ufunc. Для этого урока достаточно трех свойств ufunc:

1. функция принимает скалярные элементы и применяет один механизм ко всему массиву;
2. перед вычислением она согласует формы операндов по правилам broadcasting;
3. по `dtype` входов она выбирает подходящую реализацию и `dtype` выхода.

Оператор обычно является удобной записью ufunc:

```python
np.testing.assert_array_equal(left + right, np.add(left, right))
```

Broadcasting не является правилом любой функции NumPy. Например:

- `*` вызывает поэлементное умножение и использует broadcasting;
- `@` выполняет матричное умножение и имеет отдельный контракт внутренних осей;
- `sum(axis=...)` является редукцией и удаляет оси;
- `reshape` меняет представление осей, но не выполняет поэлементную арифметику.

### Скаляр — первый случай broadcasting

Нульмерный массив имеет форму `()`:

```python
scalar = np.array(10)
matrix = np.array([[1, 2], [3, 4]])

assert scalar.shape == ()
assert (matrix + scalar).shape == (2, 2)
np.testing.assert_array_equal(matrix + scalar, [[11, 12], [13, 14]])
```

Для каждой координаты матрицы используется единственное значение скаляра. Обычное
Python-число ведет себя в такой операции сходным образом:

```python
np.testing.assert_array_equal(matrix * 2, [[2, 4], [6, 8]])
```

Но Python-скаляр и нульмерный NumPy-массив могут по-разному участвовать в продвижении
типов. Эту деталь мы не будем запоминать по таблице: для критичной операции проверим
`np.result_type` и фактический `dtype` результата.

### Вектор не является ни строкой, ни столбцом

Одномерный массив содержит одну ось:

```python
vector = np.array([10, 20, 30])

assert vector.shape == (3,)
assert vector.ndim == 1
```

В его форме нет отдельного признака «горизонтальный» или «вертикальный». Поэтому `.T` не
добавляет ось:

```python
assert vector.T.shape == (3,)
```

Строку и столбец нужно создать явно:

```python
row = vector[np.newaxis, :]
column = vector[:, np.newaxis]

assert row.shape == (1, 3)
assert column.shape == (3, 1)
```

Их broadcasting различается:

```text
row:       (1, 3)
column:    (3, 1)
result:    (3, 3)
```

```python
outer_sum = row + column

np.testing.assert_array_equal(
    outer_sum,
    [
        [20, 30, 40],
        [30, 40, 50],
        [40, 50, 60],
    ],
)
```

Это уже не применение одного вектора к другому. Каждая строка и каждый столбец образуют
все попарные сочетания.

### Формы выравниваются справа налево

Перед сравнением NumPy мысленно дополняет более короткую форму единицами **слева**:

```text
values:     (2, 3, 2)
parameter:        (2,)
aligned:    (1, 1, 2)
```

Затем длины сравниваются справа налево. Две выровненные длины совместимы, если:

1. они равны;
2. левая равна `1`;
3. правая равна `1`.

Результирующая длина определяется не общей формулой `max`. Правильный алгоритм:

```text
если a == b: результат a
иначе если a == 1: результат b
иначе если b == 1: результат a
иначе: формы несовместимы
```

Различие важно для пустой оси:

```text
(0,) и (1,) -> (0,)
```

Единичная ось расширяется до длины другой оси, даже если та равна нулю. Выражение
`max(0, 1)` ошибочно предсказало бы `(1,)`.

### Как получить итоговую форму

Пример с матрицей и вектором:

```text
matrix: (100, 3)
vector:      (3,)
aligned: (1, 3)
result: (100, 3)
```

Последние длины равны `3`. Отсутствующая первая ось вектора считается единичной и
расширяется до `100`.

Пример двух единичных осей:

```text
left:   (5, 1)
right:  (1, 6)
result: (5, 6)
```

Пример конфликта:

```text
matrix: (100, 3)
vector:    (100,)
aligned: (1, 100)
```

Последняя пара `3` и `100` несовместима. NumPy не пытается догадаться, что `100`
возможно относится к строкам.

### Координата единичной оси всегда равна нулю

Пусть выполняется операция:

```text
values shape:    (2, 3)
parameter shape: (1, 3)
result shape:    (2, 3)
```

Для результата `result[i, j]`:

```text
result[i, j] = values[i, j] operation parameter[0, j]
```

Первая ось `parameter` имеет длину один, поэтому независимо от `i` используется координата
`0`. Именно это означает логическое расширение единичной оси.

Если ось отсутствовала, эффект тот же. Вектор `(3,)` мысленно становится `(1, 3)`.

### NumPy сопоставляет позиции, а не предметные имена

Вернемся к форме:

```text
daily_metrics: (store=2, day=3, metric=2)
parameter:                              (2,)
```

NumPy применит `parameter` к последней оси `metric`. Он не хранит наши имена осей.

Теперь предположим, что два числа параметра относятся не к метрикам, а к магазинам:

```text
parameter: (store=2,)
```

Форма по-прежнему совместима, потому что длина последней оси `metric` случайно тоже равна
двум. Операция выполнится без ошибки и применит магазинные параметры к метрикам.

Чтобы применить параметр магазина ко всем дням и метрикам, расположите его на оси
`store` явно:

```python
store_adjustment = np.array([0.9, 1.1])[:, np.newaxis, np.newaxis]

assert store_adjustment.shape == (2, 1, 1)
adjusted = daily_metrics * store_adjustment
assert adjusted.shape == daily_metrics.shape
```

Предметные имена осей являются нашим внешним контрактом. NumPy проверяет только позиции и
длины.

### `keepdims` сохраняет правильные позиции

Предыдущий урок подготовил:

```python
metric_means = daily_metrics.mean(axis=(0, 1), keepdims=True)
assert metric_means.shape == (1, 1, 2)
```

Форма уже выровнена с `(store, day, metric)`. Обе свернутые оси имеют длину один и
расширяются, а `metric` совпадает:

```text
values: (2, 3, 2)
means:  (1, 1, 2)
result: (2, 3, 2)
```

Без `keepdims` получится `(2,)`. Здесь это тоже работает, потому что `metric` является
последней осью. Но форма `(1, 1, 2)` лучше сохраняет происхождение параметра и не зависит
от мысленного добавления двух осей.

### Broadcasting и `dtype` — независимые контракты

Совместимые формы отвечают только на вопрос, какие элементы можно сопоставить. После этого
ufunc должен выбрать реализацию для типов входов.

Для каждой операции задайте три вопроса:

1. **Определена ли операция для этой пары `dtype`?**
2. **Какой `dtype` получится?**
3. **Безопасны ли диапазон значений и запись результата обратно?**

Несколько характерных случаев в NumPy 2.x:

| Входы и операция | Типичный результат | Что проверить |
|---|---|---|
| integer `+` integer | integer после продвижения | переполнение остается возможным |
| `int32 + float32` | `float64` | точность и память результата |
| integer `/` integer | `float64` | деление не остается целочисленным |
| любое числовое сравнение | `bool` | форма маски совпадает с broadcast-формой |
| `bool + bool` | `bool` | для логики яснее `&`, `|`, `logical_*` |
| `datetime64 - datetime64` | `timedelta64` | единицы времени результата |
| `datetime64 + timedelta64` | `datetime64` | согласованность временных единиц |
| строки `-` число | операция не определена | не все `dtype` поддерживают каждую ufunc |
| `object` | зависит от Python-объектов | производительность и поведение не гарантируются NumPy-типом |

Проверим несколько результатов:

```python
integers = np.array([1, 2], dtype=np.int32)
floats = np.array([0.5, 1.5], dtype=np.float32)

assert (integers + floats).dtype == np.dtype("float64")
assert (integers / integers).dtype == np.dtype("float64")
assert (integers < 2).dtype == np.dtype("bool")
```

`np.result_type` предсказывает общий тип входных значений:

```python
assert np.result_type(integers.dtype, floats.dtype) == np.dtype("float64")
```

Но это не всегда тип конкретной операции. Для двух `int32` общий тип остается `int32`, а
`np.true_divide` возвращает `float64`. Сравнение всегда возвращает `bool`, а вычитание дат
— интервал. Поэтому проверяем и общий тип, и фактический контракт ufunc.

### Булевы значения: арифметика возможна, смысл не следует автоматически

В NumPy арифметика двух булевых массивов может быть определена:

```python
left_mask = np.array([True, False])
right_mask = np.array([True, True])

assert (left_mask + right_mask).dtype == np.dtype("bool")
```

Но `+` плохо выражает намерение объединить условия или посчитать истины:

```python
union = left_mask | right_mask
both = left_mask & right_mask
true_count = left_mask.sum()
```

Операция, разрешенная библиотекой, не обязательно является лучшим предметным контрактом.

### In-place операция предъявляет дополнительные требования

Обычное выражение создает результат новой формы и типа:

```python
integers = np.array([[1, 2], [3, 4]], dtype=np.int32)
result = integers + 0.5

assert result.dtype == np.dtype("float64")
```

Но `integers += 0.5` пытается записать вещественный результат обратно в `int32` и обычно
завершается ошибкой приведения. Для in-place операции нужны одновременно:

- итоговая broadcast-форма, которая точно помещается в форму левого операнда;
- допустимое приведение выходного `dtype` обратно в `dtype` левого операнда.

Еще один пример:

```python
left = np.zeros((3, 1))
right = np.ones((3,))

assert (left + right).shape == (3, 3)
```

`left += right` не может сохранить `(3, 3)` в буфере формы `(3, 1)`, хотя обычное
сложение формально допустимо.

### Логическое расширение не равно физическому копированию

Вектор `(metric=2,)` логически используется шесть раз при операции с
`(store=2, day=3, metric=2)`, но NumPy не обязан создавать шесть физических копий до
операции.

Это можно увидеть через `np.broadcast_to`:

```python
parameter = np.array([10.0, 1000.0])
expanded = np.broadcast_to(parameter, (2, 3, 2))

assert expanded.shape == (2, 3, 2)
assert np.shares_memory(parameter, expanded)
assert not expanded.flags.writeable
```

Broadcast-представление обычно является read-only view. Несколько логических координат
могут ссылаться на один исходный элемент, поэтому запись через такое представление была бы
неоднозначной.

`np.tile` и `np.repeat` физически строят повторения и нужны только тогда, когда отдельные
значения действительно должны существовать в памяти. Для обычной поэлементной операции
они чаще скрывают контракт и расходуют лишнюю память.

### Результат операции все равно может быть большим

Broadcasting экономит память малого операнда, но обычная арифметика создает итоговый
массив:

```text
left:   (50_000,      1, 100)
right:  (     1, 50_000, 100)
result: (50_000, 50_000, 100)
```

Это `250 000 000 000` элементов. При `float64` результат потребует примерно два
терабайта. Формы совместимы, но вычисление неприемлемо.

До запуска оцените:

```text
result_elements = product(result_shape)
result_bytes = result_elements * result_dtype.itemsize
```

Для оценки нужен именно выходной `dtype`, а не тип одного из входов.

## Соберите это

### Шаг 1. Воспроизведите одинаковые формы

Сначала реализуем поэлементное сложение без NumPy и без broadcasting:

```python
def add_same_shape(left, right):
    if len(left) != len(right):
        raise ValueError("row counts differ")
    result = []
    for left_row, right_row in zip(left, right):
        if len(left_row) != len(right_row):
            raise ValueError("column counts differ")
        result.append(
            [left_value + right_value for left_value, right_value in zip(left_row, right_row)]
        )
    return result


assert add_same_shape([[1, 2]], [[10, 20]]) == [[11, 22]]
```

Здесь каждая координата обоих операндов существует явно.

### Шаг 2. Объедините две выровненные длины

```python
def merge_lengths(left, right):
    if left == right:
        return left
    if left == 1:
        return right
    if right == 1:
        return left
    raise ValueError("incompatible lengths")
```

```python
assert merge_lengths(3, 3) == 3
assert merge_lengths(1, 5) == 5
assert merge_lengths(5, 1) == 5
assert merge_lengths(0, 1) == 0
```

Последняя проверка показывает, почему нельзя использовать `max(left, right)`.

### Шаг 3. Предскажите общую форму

```python
def broadcast_shape(left, right):
    width = max(len(left), len(right))
    aligned_left = (1,) * (width - len(left)) + left
    aligned_right = (1,) * (width - len(right)) + right

    return tuple(
        merge_lengths(left_length, right_length)
        for left_length, right_length in zip(aligned_left, aligned_right)
    )
```

```python
assert broadcast_shape((100, 3), (3,)) == (100, 3)
assert broadcast_shape((5, 1), (1, 6)) == (5, 6)
assert broadcast_shape((0,), (1,)) == (0,)
```

Готовый артефакт обобщает механизм на несколько форм и сообщает, на какой паре длин
возник конфликт.

### Шаг 4. Постройте таблицу выравнивания

Для каждого операнда запишите:

1. исходную форму;
2. добавленные слева единицы;
3. предметное имя каждой выровненной оси;
4. оси, которые логически расширятся;
5. итоговую длину.

Пример:

```text
output axis       store        day       metric
values length       2           3           2
means length        1           1           2
result length       2           3           2
means expands       yes         yes         no
```

Если в одной позиции оказываются `store=2` и `metric=2`, длины совместимы, но таблица
должна выдать семантическое предупреждение.

### Шаг 5. Разделите общий тип и тип операции

```python
left_dtype = np.dtype("int32")
right_dtype = np.dtype("int32")

common_dtype = np.result_type(left_dtype, right_dtype)
operation_dtype = np.true_divide(
    np.ones((), dtype=left_dtype),
    np.ones((), dtype=right_dtype),
).dtype

assert common_dtype == np.dtype("int32")
assert operation_dtype == np.dtype("float64")
```

Нульмерные образцы позволяют спросить ufunc о типе выхода, не создавая большой
broadcast-результат. Для реальных значений дополнительно нужны проверки диапазона,
деления на ноль и конечности — они появятся в уроке о численной точности.

## Используйте это

### Примените агрегаты прошлого урока

```python
metric_means = daily_metrics.mean(axis=(0, 1), keepdims=True)
metric_stds = daily_metrics.std(axis=(0, 1), keepdims=True)

if np.any(metric_stds == 0):
    raise ValueError("constant metric cannot be standardized")

standardized = (daily_metrics - metric_means) / metric_stds

assert standardized.shape == daily_metrics.shape
np.testing.assert_allclose(
    standardized.mean(axis=(0, 1)),
    [0.0, 0.0],
    atol=1e-12,
)
np.testing.assert_allclose(
    standardized.std(axis=(0, 1)),
    [1.0, 1.0],
    atol=1e-12,
)
```

Broadcasting здесь решает только применение параметров. Методология среднего,
стандартного отклонения, пропусков и `ddof` остается контрактом предыдущего урока.

### Примените параметры выбранной предметной оси

Параметр каждой метрики может оставаться вектором, потому что `metric` — последняя ось:

```python
metric_weight = np.array([1.0, 0.001])
weighted = daily_metrics * metric_weight

assert metric_weight.shape == (2,)
assert weighted.shape == (2, 3, 2)
```

Параметр магазина нужно поставить на первую ось:

```python
store_weight = np.array([0.9, 1.1])[:, np.newaxis, np.newaxis]

assert store_weight.shape == (2, 1, 1)
assert (daily_metrics * store_weight).shape == daily_metrics.shape
```

Параметр дня ставится на среднюю ось:

```python
day_weight = np.array([1.0, 0.8, 1.2])[np.newaxis, :, np.newaxis]

assert day_weight.shape == (1, 3, 1)
assert (daily_metrics * day_weight).shape == daily_metrics.shape
```

Позиция единичных осей выражает, к какому измерению относится параметр.

### Постройте условие с порогом каждой метрики

`np.where` согласует форму условия и двух вариантов результата:

```python
thresholds = np.array([9.0, 1000.0])
above_threshold = daily_metrics >= thresholds

assert above_threshold.shape == daily_metrics.shape
assert above_threshold.dtype == np.dtype("bool")

flagged = np.where(above_threshold, daily_metrics, np.nan)
assert flagged.shape == daily_metrics.shape
assert flagged.dtype == np.dtype("float64")
```

Здесь broadcasting используется дважды: при сравнении с порогами и при выборе между
числовым массивом и скаляром `NaN`.

### Постройте попарные разности осознанно

```python
left = np.array([10.0, 20.0, 30.0])[:, np.newaxis]
right = np.array([8.0, 25.0])[np.newaxis, :]

differences = left - right

assert left.shape == (3, 1)
assert right.shape == (1, 2)
assert differences.shape == (3, 2)
```

Каждый элемент результата представляет одну пару `(left_item, right_item)`. Такой прием
полезен для расстояний и сравнений, но размер результата растет как произведение длин.

### Используйте официальные предикторы

```python
assert np.broadcast_shapes((2, 3, 2), (2,)) == (2, 3, 2)
assert np.broadcast_shapes((5, 1), (1, 6), (6,)) == (5, 6)
```

`np.broadcast_shapes` не создает результат и подходит для ранней проверки памяти.

`np.broadcast_arrays` возвращает согласованные представления нескольких массивов, а
`np.broadcast_to` — представление одного массива в заданной форме. Они полезны для
диагностики, но в обычной формуле явное построение представлений чаще не требуется.

### Запустите контракт операции

Артефакт принимает две формы, имена осей и `dtype`, но не выделяет память под результат:

```bash
uv run --locked python phases/02-numpy/07-broadcasting/outputs/broadcast_contract.py \
  --operands '[
    {"name":"daily_metrics","shape":[2,3,2],"axis_names":["store","day","metric"],"dtype":"float64"},
    {"name":"metric_means","shape":[1,1,2],"axis_names":["summarized_store","summarized_day","metric"],"dtype":"float64"}
  ]' \
  --operation subtract
```

Отчет содержит:

- выровненные позиции осей и признак логического расширения;
- итоговые `shape` и предметные имена осей;
- коэффициент логического использования каждого операнда;
- общий входной `dtype` и тип конкретной операции;
- оценку числа элементов и байтов результата;
- отдельные проверки формы и типа для in-place записи;
- предупреждения о конфликте имен осей, integer overflow, булевой арифметике и памяти.

## Сломайте это

### Несовместимые последние оси

```python
np.zeros((4, 3)) + np.zeros(4)
```

После выравнивания последние длины `3` и `4` конфликтуют. Намерение применить параметр к
строкам не меняет позиционных правил.

### Совместимые длины с разным смыслом

```text
values:    (store=2, day=3, metric=2)
parameter:                         (store=2,)
```

NumPy видит совпадающие двойки и выполняет операцию по `metric`. Только реестр предметных
осей обнаруживает, что параметр магазина попал не на ту позицию.

### Попытаться транспонировать одномерный вектор

```python
parameter = np.array([0.9, 1.1])
wrong = parameter.T

assert wrong.shape == (2,)
```

Для оси `store` трехмерного массива нужна форма `(2, 1, 1)`, а не `.T`.

### Добавить единичную ось не в ту позицию

```python
store_weight = np.array([0.9, 1.1])

wrong = store_weight[np.newaxis, np.newaxis, :]  # (1, 1, 2): metric
right = store_weight[:, np.newaxis, np.newaxis]  # (2, 1, 1): store
```

Обе формы совместимы с `(2, 3, 2)`, но применяют значения к разным осям.

### Принять `np.result_type` за тип любой операции

```python
assert np.result_type(np.int32, np.int32) == np.dtype("int32")

result = np.array([1], dtype=np.int32) / np.array([2], dtype=np.int32)
assert result.dtype == np.dtype("float64")
```

Продвижение входов и выход конкретной ufunc — связанные, но не идентичные контракты.

### Использовать арифметику вместо булевой логики

`mask_a + mask_b` для `bool` может выполниться, но не выражает однозначно ни логическое
«ИЛИ», ни количество истинных условий. Используйте `|`, `&`, `np.logical_or` или явную
редукцию маски.

### Ожидать, что любая операция поддерживает любой `dtype`

```python
labels = np.array(["A", "B"])
labels - 1  # операция не определена
```

Broadcast-совместимость формы `(2,)` со скаляром не создает реализацию вычитания строк и
чисел.

### Записать расширенный результат на месте

```python
left = np.zeros((3, 1))
right = np.ones(3)

assert (left + right).shape == (3, 3)
left += right  # результат формы (3, 3) не помещается в (3, 1)
```

In-place операция не может изменить форму буфера левого операнда.

### Записать float обратно в integer

```python
integers = np.array([1, 2, 3], dtype=np.int32)
integers += 0.5  # небезопасное приведение float64 -> int32
```

Обычная операция создала бы новый вещественный массив. In-place контракт строже.

### Переполнить integer после успешного согласования

```python
values = np.array([120], dtype=np.int8)
increment = np.array([20], dtype=np.int8)
result = values + increment
```

Формы и типы согласованы, но диапазона `int8` недостаточно. Broadcasting не проверяет
значения. Подробные проверки overflow и tolerances будут в уроке о численной точности.

### Создать огромный попарный результат

```text
(50_000, 1, 100) и (1, 50_000, 100)
```

Малые операнды не копируются заранее, но результат `float64` требует около двух терабайт.
Проверяйте форму, количество элементов и `dtype` до арифметики.

### Использовать `tile` для сокрытия непонятной формы

Физическое повторение может заставить формы совпасть, но не доказывает правильность осей.
Сначала назовите назначение параметра и вставьте единичные оси. `tile` оставьте случаям,
где отдельная физическая копия действительно является требуемым результатом.

## Проверьте это

Перед каждой нетривиальной операцией пройдите семь шагов:

1. Назовите поэлементную операцию и убедитесь, что это не редукция или `@`.
2. Запишите формы и предметные имена осей всех операндов.
3. Дополните более короткие формы единицами слева.
4. Сравните каждую позицию и получите итоговую форму.
5. Для каждой расширяемой оси объясните, какая координата будет повторно использована.
6. Проверьте допустимость операции и выходной `dtype`.
7. Оцените память результата и возможность in-place записи, если она используется.

После вычисления проверьте:

- фактический `shape` равен прогнозу `np.broadcast_shapes`;
- каждая выходная ось имеет ожидаемый предметный смысл;
- контрольная координата рассчитана вручную;
- `dtype` совпадает с контрактом конкретной операции;
- результат конечен там, где это требуется;
- операция над небольшим срезом совпадает с явным циклом;
- попарный результат не создан случайно вместо поэлементного.

Запустите behavioral tests урока:

```bash
uv run --locked python -m unittest discover \
  -s phases/02-numpy/07-broadcasting/tests \
  -v
```

Они проверяют одинаковые формы, скаляры, векторы, несколько операндов, пустые оси,
предметные имена, `keepdims`, конфликты смысла, смешанные `dtype`, деление, сравнения,
даты, булевы значения, in-place контракт, память и CLI.

Исполняемый пример:

```bash
uv run --locked python phases/02-numpy/07-broadcasting/code/main.py
```

## Поставьте результат

`outputs/broadcast_contract.py` — проверяемый контракт поэлементной broadcast-операции.
Он анализирует две спецификации без создания результата:

```python
report = analyze_broadcast(
    [
        {
            "name": "daily_metrics",
            "shape": [2, 3, 2],
            "axis_names": ["store", "day", "metric"],
            "dtype": "float64",
        },
        {
            "name": "metric_means",
            "shape": [1, 1, 2],
            "axis_names": ["summarized_store", "summarized_day", "metric"],
            "dtype": "float64",
        },
    ],
    operation="subtract",
    memory_limit_mb=256,
)
```

Артефакт полезен перед расчетом с новыми формами: он делает видимыми позиционное
выравнивание, семантические оси, расширение, тип ufunc и стоимость результата.

## Упражнения

1. Вручную выведите результат форм `(8, 1, 6, 1)` и `(7, 1, 5)`, затем сопоставьте
   каждую выходную ось с исходной и проверьте ответ через `np.broadcast_shapes`.
2. Для массива `(region, store, day, metric)` расположите отдельно параметры региона,
   дня и метрики. Добейтесь одинаковой итоговой формы и объясните различие предметного
   смысла трех операций.
3. Добавьте в артефакт операцию `power`. Проверьте целые и вещественные входы, отрицательную
   степень, выходной `dtype`, in-place запись и оценку памяти без создания большого массива.

## Ключевые термины

| Термин | Распространенное заблуждение | Точное значение |
|---|---|---|
| Поэлементная операция | «Любая функция над массивом» | Один скалярный механизм, применяемый к сопоставленным координатам |
| Ufunc | «Только быстрая математическая функция» | NumPy-функция с правилами типов, broadcasting и поэлементного выполнения |
| Broadcasting | «Физическое копирование малого массива» | Позиционные правила логического согласования форм операндов |
| Trailing axis | «Первая ось массива» | Ось в правой части `shape`, с которой начинается сравнение |
| Singleton axis | «Пустая ось» | Ось длины `1`, единственная координата которой может использоваться повторно |
| Неявная ведущая ось | «Новая ось в исходном массиве» | Мысленная единичная ось, добавленная слева только для выравнивания форм |
| Семантическая совместимость | «Совпадение длин» | Соответствие выровненных позиций одним и тем же предметным осям |
| Продвижение типов | «Broadcasting меняет `dtype`» | Отдельные правила выбора общего типа входов для операции |
| In-place операция | «То же выражение без создания переменной» | Запись результата обратно в существующие форму и `dtype` левого операнда |
| Логическая форма | «Объем уже выделенной памяти» | Форма доступных координат, которая может быть представлением без копии |

## Дополнительное чтение

### На русском языке

- [Broadcasting в NumPy — русская версия руководства](https://runebook.dev/ru/docs/numpy/user/basics.broadcasting) — начните с правил сравнения форм справа налево, примеров со скаляром, вектором и `np.newaxis`; затем повторите те же правила по официальной английской версии ниже.
- [Операции над массивами: русскоязычный справочник NumPy](https://numpy.ru/spravochnoe-rukovodstvo/operaczii-nad-massivami/) — карта функций `broadcast`, `broadcast_to`, `broadcast_arrays`, `expand_dims` и других операций изменения формы; полезна для выбора инструмента после ручного прогноза осей.
- [Типы (`dtype`) и преобразования в NumPy](https://docs-python.ru/packages/biblioteka-numpy-v-python/dtype-i-preobrazovaniya/) — русскоязычное продолжение линии про promotion, выходной тип `ufunc`, безопасное приведение и ограничения целых типов.
- [Основы NumPy: массивы и векторные операции](https://slemeshevsky.github.io/python-course/numpy/pdf/numpy.pdf) — развернутый учебный конспект на русском: сопоставьте разделы о поэлементных функциях, broadcasting и формах с предметными осями этого урока.

### На английском языке

- [NumPy User Guide: Broadcasting](https://numpy.org/doc/stable/user/basics.broadcasting.html) — главный официальный источник: точные правила совместимости, схемы многомерных форм, `newaxis`, попарные операции и пример неэкономного большого результата.
- [numpy.broadcast_shapes](https://numpy.org/doc/stable/reference/generated/numpy.broadcast_shapes.html) — официальный предиктор общей формы нескольких операндов без создания массивов; используйте для проверки ручного рассуждения и раннего отказа.
- [numpy.broadcast_to](https://numpy.org/doc/stable/reference/generated/numpy.broadcast_to.html) — документация о явном broadcast-представлении, его read-only статусе и нескольких логических элементах, которые могут ссылаться на одну область памяти.
- [Universal functions (`ufunc`)](https://numpy.org/doc/stable/reference/ufuncs.html) — официальный каталог поэлементных операций и их общего контракта: broadcasting, `dtype`, `out`, `where`, `casting` и доступные сигнатуры типов.
- [Data type promotion in NumPy](https://numpy.org/doc/stable/reference/arrays.promotion.html) — правила сочетания `bool`, integer, float, complex и Python-скаляров; особенно полезно для объяснения неожиданных типов и переполнения.
- [numpy.can_cast](https://numpy.org/doc/stable/reference/generated/numpy.can_cast.html) — точные режимы `no`, `equiv`, `safe`, `same_kind` и `unsafe`, необходимые для проверки возможности записать выходной `dtype` обратно в существующий массив.
