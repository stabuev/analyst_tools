# Решение о версии Python

Документ фиксирует три разные вещи: какой Python поддерживает проект, какую линию должен
выбрать локальный инструмент и какой интерпретатор действительно запустил проверяемый
процесс. Не вставляйте сюда секреты и локальные пути с именами пользователей.

## 1. Контекст проекта

- **Проект:**
- **Какая Python-задача появится в проекте:**
- **Кто должен суметь запустить проект:**
- **Где будет выполняться код:** локальный терминал / notebook / CI / другое

## 2. Наблюдение за текущим процессом

| Что проверили | Команда или место проверки | Наблюдение | Что это означает |
|---|---|---|---|
| Версия в shell | `python3 --version` | | |
| Команда, найденная shell | `command -v python3` | | |
| Версия внутри процесса | `sys.version_info` | | |
| Интерпретатор процесса | `sys.executable` | | |

Если путь содержит имя пользователя, в публикуемой версии замените домашнюю часть на
`<home>`. Для диагностики сохраните достаточно информации, чтобы отличить системный
Python от проектного окружения.

## 3. Как читается номер версии

- **Major:**
- **Minor:**
- **Patch:**
- **Какая часть важна для выбора линии совместимости и почему:**

## 4. Контракт проекта

| Решение | Значение | Какую задачу решает | Чего не гарантирует |
|---|---|---|---|
| `requires-python` | | | |
| `.python-version` | | | |
| Фактически запущенный Python | | | |

## 5. Обоснование

- **Почему выбрана такая минимальная версия:**
- **Почему выбранная локальная линия находится внутри требования проекта:**
- **На каких версиях проект действительно проверялся:**
- **Что пока остаётся предположением, а не доказанной совместимостью:**

## 6. Сценарий несовпадения

Опишите, что будете проверять, если коллега сообщит другую версию или путь Python.

1. **В каком именно процессе проявилась ошибка:**
2. **Как узнать его версию и путь интерпретатора:**
3. **С чем сравнить наблюдение:**
4. **Что нельзя менять только ради исчезновения ошибки:**
5. **Какое действие станет следующим после диагностики:**

## 7. Вывод для reviewer

В трёх-пяти предложениях объясните:

- какой Python допускает проект;
- какую линию предлагает локальный selector;
- какой Python был запущен при проверке;
- совпадают ли эти три части;
- что ещё потребуется для доказательства воспроизводимости.
