name: snapshot_project
version: "1.0.0"
config-version: 2

profile: snapshot_project

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  snapshot_project:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table

data_tests:
  snapshot_project:
    +store_failures: false

snapshots:
  snapshot_project:
    +target_schema: snapshots
